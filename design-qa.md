# 会中正文与原话边注 — 视觉 QA

final result: passed

## 对照依据与状态

- 视觉目标：[用户确认的第一张方向之收窄细化稿](docs/design/assets/meeting-editorial-reference.png)。原图1489×1056px，按保持比例缩至1488×1055用于比较。
- 实际实现：[Electron正文／原话](tests/results/meeting-editorial/desktop-sources.png)，1488×1056 CSS视口，最终截图使用CSS像素1488×1056；早期2976×2112的Retina截图先归一到同一尺寸。
- [全页并排比较](tests/results/meeting-editorial/comparison.png)与[原话局部比较](tests/results/meeting-editorial/comparison-sources.png)均已实际查看。较窄侧栏和主内容位置通过[几何测量](tests/results/meeting-editorial/geometry.json)核验。
- 状态：同一合成试点讨论，原话展开并选中支持条件的来源，展示全部三段原话。生产截图来自真实Electron；没有真实音频或真实模型输出。
- 意图差异：概念稿的收听状态／经过时长不在未采音测试中伪造；示例姓名、数量、共识均不补造。实际原话时间来自合成输入，保持“发言人未确认”。

## 比较历史与修复

1. 第一轮：发现正文标题前重复日期／处理信息、表格级引用重复、窄窗关闭焦点早于inert释放。已将元信息下移，块级来源收为轻量图标＋数量，保留行内编号，焦点在释放背景后返回。
2. 第二轮：实际滚动时顶栏离开视野，旧样式留下整块蓝边。已改固定在顶部的会议工具栏并移除整块边线；来源选择使用原话边线和对应标题／行的细下划线。重新截图比较，头部完整、正文无外层卡片、主次关系一致。
3. 第三轮：真实800×600窗口在200%时，CSS视口400×284，旧布局只剩很少原话阅读高度。现改为有效高度≤500px的全高原话模态，保留关闭与内部滚动；[原生缩放截图](tests/results/meeting-editorial/zoom-200.png)和[尺寸证据](tests/results/meeting-editorial/zoom-200.json)已检查，原话面板从180增至284 CSS px高，纠错内容可读。首次Playwright scale截图裁切不作为最终证据，最终使用同一Electron窗口的capturePage原生截图1600×1136。

## 五项保真检查

| 表面 | 结果／依据 |
|---|---|
| 字体排版 | 系统无衬线、正文15px、原话15px／1.75、标题30px和组标题17px；符合确认后的可读性参数。概念图字阶近似，不把生成图片中偏大的字逐像素照搬。中英文与真实200%截图均检查。 |
| 间距布局 | 1488宽时原话372px、分隔x1116；正文x48、宽1020，开关面板横向坐标与宽度不变。无外层大圆角卡、无嵌套卡片和表格斑马底。极窄窗有明确降级。 |
| 色彩 | 继承已校准collaborative-light-v2：白色正文、极浅原话面、深正文、克制蓝色引用；状态兼有文字。没有新增全页玻璃、渐变或持续动效。未声称整套屏幕阅读器／WCAG全面认证。 |
| 图像／图标 | 当前设计不需要新位图内容。沿用真实Lucide图标和文字品牌，未用截图充当交互界面；来源、反馈、纠错均为实际控件。 |
| 文案／内容 | 示例比较表及待核实项来自合成provider，不存在生产硬编码。引用精确到ID＋revision，个人反馈仅形成待发送草稿。原话没有可靠字符范围时以片段边线指示，不虚构关键句定位。 |

## 实际交互核验

个人request不混入全部转写，显式引用仍可追溯；主动历史切换立即解除阅读锁；编号定位、相关／全部转写切换、后台更新但当前正文保留、主动查看更新、反馈追加保留现有草稿，不自动发送，纠错冲突就地提示并防重复保存、纠错草稿关闭重开、历史引用不被新revision替换、长转写回看不自动拉底、回到最新、窄窗inert与焦点返回均通过Electron专项。已有表格、关系图、时间线、图表、HTML／SVG、个人试算、结束恢复和设置回归分开记录到[结果](tests/results/meeting-editorial-validation.md)。页面错误专项记录为0。

## 剩余边界

没有待修的P0／P1／P2视觉问题。概念中的精确句内高亮需有可靠字符范围才可提供；目前明确定位整段来源。普通阅读仍自动跟随，核对来源及选中块才保护阅读，未宣称全场景重排队列。真实转写／模型质量、Windows真机字体／DPI和完整屏幕阅读器是独立验收，不由本次静态及合成结果替代。

最终Mac成品包另有[真实截图](tests/results/meeting-editorial/mac-package.png)及[与参考并排图](tests/results/meeting-editorial/package-comparison.png)，包内同一372px布局、原话引用和非开发入口已核验。
