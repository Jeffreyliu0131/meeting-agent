# 两新分支整合验证

2026-09-12，macOS arm64，Node22.14.0／Electron44.3.0。原main5878c20；整合FTY5943d23与xuwenzhe b6d7061，代码主提交5d7e6ab。对应[session](../../docs/sessions/2026-09-12-branch-integration.md)。

## 本轮变化

FTY私有草稿移到Meeting.intentPreparation，旧库兼容；显式转为正式协作预览，不自动发放或把口头姓名绑定身份。手工锁、删除项、源版本与已编辑正式组件受保护。启用私有自动准备时，同次理解不重复生成另一套自动组件。

用户选会的集合与现有协作／转写同事务保存。正式协作决定进入汇总，引用是业务确认记录而非原话。报告绑定生成起点的成员与版本；期间变化拒绝旧结果，旧报告保留；集合表单保存统一为单事务。Live Transcribe首包／尾音／取消修复与双音轨并发保持。

本地模型代理仍显式opt-in且仅开发运行；异步启动不阻塞UI、退出取消晚到启动、进程错误受控、配置密钥不进入日志。没有执行真实代理安装或付费模型调用。

## 实际验证

- 两分支组合210项单元通过；最后集合单事务修改后22项相关单元再通过。新跨模块回归覆盖四类转交、旧FTY库迁移、集合不丢失、确认记录引用、生成期间变化拒绝；代理使用本地假进程验证退出／密钥隐藏。
- 完整Electron 26项通过，包含现有协作、Live STT、提醒、设置及FTY私有草稿；新增集合操作专项通过，最终私有草稿转正式组件及重启恢复专项通过。
- 加入保留的本地会中精修／试算等成果后221单元通过；受影响8项桌面均有通过证据：集合1、会中4、试算2、意图转交1。初次8项运行7通过，意图用例在截图／退出期间超时并伴worker收尾超时；统一为既有cleanupElectron、去除重复截图后意图与试算3项通过。没有宣称首次8/8整轮通过，也不以测试强制退出证明正常应用退出验收。
- 新集合测试初次使用不存在的New collection入口以及未渲染摘要断言，另有安全清理前缀不符；已按实际选择会议→新建集合→报告正文／状态修正并通过。这些失败未作为产品缺陷隐藏。
- build、TypeScript、修改文件格式和文档入口检查通过。两个来源分支的实测历史不覆盖本轮真实模型质量。
- [双端候选包](branch-integration-packages.json)：133文件指纹检查，14个dist文件及整个app.asar一致；Mac arm64与Windows x64架构正确，实际Mac包隔离启动及协作烟测通过。

## 边界

本地组合包含未提交的会中精修／试算成果，未将这些源码归入本次远端整合提交；首页预览仍未进入正式应用，Event评测任务由其原记录维护。Windows真机、真实语音／模型、长会议质量及跨设备协作未验。日常包回写和最终远端main以session收尾为准。


最终main为55267d6，本地与远端一致，开放PR为空。[日常包](branch-integration-daily-packages.json)已核验；[源码快照](branch-integration-source.json)保留本轮范围。同步期间新出现的实时可视化工作未纳入该快照、包或远端提交，当前工作树的这些后续变更由原任务继续验收。详见session收尾。
