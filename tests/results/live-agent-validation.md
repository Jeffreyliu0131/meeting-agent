# 连续 Agent 与会议入口验证

执行日期：2026-09-11。对应 `5fcd90f` 后本任务的未提交工作树，包含 ADR-004 与会议入口接入；首版旧报告保持历史范围。本轮在 macOS arm64 本地执行，没有外部模型调用或真实音频采集。

## 当前执行记录

- `npm test`：38项单元／领域／架构测试通过，包含理解与预览解耦、生成期间新批次、来源纠正、双轨返回乱序、慢转写队列、溢出／失败、500段长会回查、预算、patch、个人探索隔离、系统语言、标题竞争和音频设置快照。
- `npm run test:stream`：360个事件加速合成输入全部处理，最终产物追上最后输入。见 [机器结果](stream-evaluation.json)。模拟30分钟的事件数量，实际回放间隔2毫秒；这不是实际开会30分钟，也不代表真实模型时延。
- `npm run test:e2e`：最终7/7通过，用时45.7秒。覆盖正常入口、设备失效与同一事件恢复、渐进提问、自动更新及草稿保护。曾发现语言下拉框缺少明确可访问名称，修复后整套重跑通过。
- `npm run test:model`：退出码2，`MODEL_NOT_CONFIGURED`。只检查配置是否存在，不展示凭证；未执行真实模型、STT或语义评分。

- TypeScript/Vite/esbuild构建与Prettier检查通过。macOS应用目录和Windows x64目录重新生成，均已核对后台包与当前构建一致：[包内容](package-content-live.json)。macOS打包应用启动通过，空数据目录、未自动录音：[启动探针](package-smoke-live.json)。Windows仅构建，未运行。
- 代码指纹、测试数量与合成负载汇总：[live-agent-summary.json](live-agent-summary.json)。文档静态检查通过；该检查不证明产品效果。

## 证据范围

模型／STT HTTP测试服务是明确固定的测试替身，音源是测试注入振荡器。慢STT人为延迟6.5秒，超过5秒切片间隔，验证队列继续工作及停止释放。正常入口测试禁用开发入口，验证一次保存设置、两场一键开始、活动事件复用、空状态无提问和产物出现后主动展开。

真实模型评估脚本已改为按间隔送入新内容，不等待上一轮响应；结束后等待处理完成，保存最终产物、调用账目及待人工核对的expected条目。脚本本身不自动宣布语义通过，也不包含实际Electron预览。

## 未验证

真实中英／混说转写、真实表达和命名质量、检索同义／隐含指代覆盖、真实端到端p95及货币成本、实际设备插拔／系统默认路由变化、线上远端与本机音源、Windows真机均未验收。当前使用切片转写，未改成Realtime接口。签名、分发和安装体验不在本轮完成声明中。

代码、执行结果与复入步骤见 [本任务记录](../../docs/sessions/2026-09-11-agent-iteration.md) 和 [ADR-004](../../docs/adr/004-live-agent-pipeline.md)。本轮不提交或推送。
