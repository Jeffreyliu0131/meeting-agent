# 静默需求识别、组件审核与自然音频验证

日期：2026-09-12，Windows，YH `a91dcce`后的未提交修正。对应[执行记录](../../docs/sessions/2026-09-12-agent-component-review.md)与[自然讨论验收表](../fixtures/collaboration-manual-test.md)。本轮未提交／推送，未重启日常应用。

## 产品及程序检查

正常音频会议自动建立本地协作目录，不额外启动录音；M从自然讨论推断需求，C接收前文来源及默认交互规则，四类组件均有完整只读预览、默认受众与共享依据，host一次审批分发。手工组装／编辑为高级可选入口。私有收集结束不等于公开发放。

- `npm run build`：通过，含TypeScript类型检查与桌面／UI构建。
- `npm test`：145项通过。新增／扩展用例覆盖默认协作、不自动采音、C前文与私有来源隔离、完整内容校验修复、收集完成进入私有ready、JSON-object供应商契约与完整请求预算。
- Electron最终回归：26项用例功能断言通过，但worker teardown超时，命令退出码1；不能记为整套命令通过。该退出问题尚未解决。新增四类组件测试通过：使用本地HTTP模型传输替身，验证自然讨论走真实M/C/R图、审核页没有可见输入框、无需保存草稿、一次审批形成公开轮、参与者异议触发冲突以及无失败任务。

最初完整回归24/26通过：一次投票测试／worker超时，单独重跑通过；一项提醒采集夹具失败，原因是HTTP批量转写替身误用了默认实时WebSocket模型。夹具现显式指定`whisper-1`，未修改产品的`gpt-live-transcribe`配置。最终回归另记，不将首次失败隐藏为通过。

## 合成文本、真实DeepSeek调用

实际使用本地配置的`deepseek-flash`，只发送本轮合成自然对话，不读取真实会议；通过真实SessionService、M/C/R及内存SQLite检查。以下不是统计意义上的准确率，也不是音频全链路测试。

| 摘录 | 观察结果 | 端到端处理时间 |
|---|---|---|
| 周末公园／历史团建，今天没有现实安排 | 未产生组件，无失败任务 | 5.580秒 |
| 三个试点范围＋不同倾向＋今天各选一个 | 完整三选一投票、单选、允许弃权、发起者截止；最终复测ready，0公开轮、无失败任务 | 33.087秒 |
| A原型／B清单／C反馈分类的自然交付安排 | 三项完整分工，A/B期限来自原话、C日期未知、页面范围为讨论点；ready、0公开轮 | 81.255秒 |
| 第一轮内部试点、一周后再定客户，A/B/C核对影响与遗漏 | 完整确认内容及指定三人，最终复测ready、0公开轮、无失败任务 | 44.769秒 |

闲聊和分工来自本轮较早的修正版本；投票和确认在发现收集状态误判后复测，确认使用最终提示词。真实模型四类全覆盖未完成，冲突链路本轮证据为程序替身测试。完整v2多段音频、持续上下文和改口的真实模型表现仍须按操作表实跑。

实调发现并修复两类问题：JSON-object接口要求请求包含字面`JSON`，原C/R只有Schema字样导致HTTP400；模型混淆“等待新选项／结论内容”和“等待参与者投票／同意”，后者应直接生成可审核组件。首次确认摘录还出现过一次分析失败；后续复测无失败任务，不据此保证所有输入稳定成功。

## TTS产物

来源为[自然讨论v2](../fixtures/collaboration-natural-dialogue.zh-CN.txt)，所有方括号标题／检查点已排除。正文没有任何对Agent说的命令，全部是参与者之间的讨论。

调用OpenAI `gpt-4o-mini-tts`、`marin`，生成14段24kHz／16bit／单声道WAV，另合并12段主线为316.9秒（含段间6秒静音）。提供不自动播放的分段HTML播放器、逐段原文、manifest及ZIP。路径`exports/collaboration-tts-20260912/`属于本地忽略产物，不提交音频或凭证。

已验证14段WAV头、声道、采样率、长度和非静音信号；未做逐字回转写或人工听辨验收。单音色AI语音不能用于说话人识别验收；A/B/C为明确的本地模拟身份。

## 未验证及交付范围

本轮没有通过日常进程真实麦克风采音，也未验证真实多人的重叠发言、跨端分发或macOS运行。第二批议程、信息收集、方案对比、风险与问题清单仍未实现。本轮要求已写回后续规划，不能把规划当交付。
