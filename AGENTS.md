# 开发协作约定

适用范围：本目录及所有子目录。方向基线：`meeting-event-generative-v3`，2026-09-11；用户已授权建立独立实现文档与代码目录，本次已授权创建GitHub仓库并首次提交、推送本目录；未授权产品开发或部署。

## 仓库边界

- 本目录是独立Git根目录，对应私有仓库`https://github.com/Jeffreyliu0131/meeting-agent`。
- 产品代码放在本目录的`src/`。仅跟踪本目录内文件；父目录资料只用于本地研究与Agent参考，不上传、不复制进仓库，不以符号链接引入。
- 提交前检查暂存文件，排除密钥、真实会议数据、录音、依赖和构建产物。

## 事实与权威

- 中文、直接、简洁。读取与建议不自动写文件；用户明确要求开发或修改后，在其范围内自主执行。
- 按`README.md`顺序阅读。`docs/product-definition.md`和`docs/decisions.md`记录最新产品认知；技术文档中的工程建议不是用户已选定的技术栈。
- 此目录记录用户在父目录旧基线之后明确补充的方向：线上＋线下、会议事件、桌面形态、Agent生成表达、工作级简洁、身份按可靠性降级。与父目录早期产品描述冲突时，按这些有来源的新确认执行；不扩大系统权限或外部操作授权。
- 不以父目录报告应用的依赖决定产品栈。不要依赖父目录文件、个人绝对路径或外部参考设计才能开发本目录。

## 产品不可退回的边界

- Agent决定表达的内容、结构、关系与必要交互；不只给固定预算卡、固定三栏或预写的会议图填数据。
- 提供通用渲染器与基础元素，不预写具体会议的最终产物。相同输入可以有不同有效表达，以语义与使用效果验收。
- 主体是一场会议事件：开始、持续理解、按需查看、主动推演、确认与结束保存。对话输入是其中一种操作。
- 线上和线下都在产品范围。首版可明确限制一个已验证桌面系统、一种线上接入方式和少量测试参与者；不能将不支持的一类会议写成已完成。
- 身份可靠才实名；声音A/B不等于姓名；无法可靠区分时保留无身份观点和分歧。不得因身份缺失阻断基础会议理解，不承诺未经实测的90%或99%准确率。
- 默认个人按需查看；浮层隐藏不停止后台。共享、多人编辑、会后代办不是正常使用前提。
- 工作级表达：少文字、清楚逻辑、来源可查、状态可辨、更新不跳动。没有值得表达的新信息时不生成。
- 原话、Agent解释、试算、个人采纳和会议决定分开；不得将沉默、建议或普通按钮点击写成全体共识。

## 实现与执行

- 产品代码在`src/`。本轮只创建文档、合成测试材料和空代码目录；不得据此安装依赖、启动采集或声称实现完成。
- 后续开发遵守版本、稳定ID、来源依赖、幂等和过期响应校验；不靠整场反复重建掩盖增量错误。
- 生成代码只在规定表达环境中处理；没有Node、文件、网络、采音、系统命令或任意业务写入权限。具体隔离边界见`docs/rendering-runtime.md`。
- 确定性数值由受校验工具计算。公式和日期口径不明确时显示未知；不让生成页面成为业务事实来源。
- 密钥仅在可信服务／操作系统凭证存储中；不进入仓库、生成内容、前端或日志。原始会议与音频默认不进仓库。
- 用真实测试报告实际能力。合成输入、手工身份、录音回放与实时输入必须分别标注。
- 文件删除／移动、发送、部署、购买、权限变更、Git提交／推送／合并、创建公开仓库，须有当前请求明确动作与目标。
- 不主动启动多Agent或创建其他任务；仅在用户明确要求或具体适用规则要求时使用。
- 验证以变更和验收要求为边界；不为文档或低影响可逆改动堆叠无意义测试。
