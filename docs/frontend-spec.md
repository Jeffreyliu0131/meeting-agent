# 前端页面、桌面入口与视觉实施规范

协作前端已接入：主工作区保留组件索引；发起者在独立置顶悬浮窗预览／编辑／发放，参与者使用绑定身份的独立悬浮窗。ready草稿在悬浮球提示，准备完成不自动抢焦点。四组件沿用[组件规范](collaboration-v1-design.md)，实际范围与未完项见[运行说明](collaboration-v1-runtime.md)。

> 文件职责：产品／工程设计要求，不是完成清单。当前进度与最新修订见[状态页](status.md)；已交付首版取舍见[ADR-003](adr/003-cross-platform-first-version.md)，实际结果见[验证记录](../tests/results/README.md)。具体实现以代码核对，未实现的要求仍是目标。
2026-09-11｜当前视觉依据为[style.md](design/style.md)中的已认可 8 张概念图，文档标识 `collaborative-light-v2`。本文件负责交互与窗口契约；style.md 负责视觉、页面分区和实施建议。本地运行参数、外壳与动态内容已统一迁移，飞书布局参考后的细化见 style.md 第 11 节；实际验证见[本轮记录](sessions/2026-09-11-frontend-refresh.md)。启动、设备、语言细则见[会议入口规范](meeting-entry-spec.md)。

设计资料导航见[design/README.md](design/README.md)。

## 1. 设计依据与优先级

用户本轮明确不提供旧图，让生成端自由设计，并认可新 8 图的整体风格。其页面和来源见[style.md](design/style.md)，替代关系见[设计决策](design/decision.md)。[旧第二张图](design/assets/selected-editorial-reference.png)仅作历史保留；新图是概念稿，不是实际运行截图。

实现依据按职责区分：用户最新确认优先；本规范、[会议入口](meeting-entry-spec.md)及[语言规范](language-spec.md)约束行为；[style.md](design/style.md)定义当前视觉和页面；[设计tokens](design/tokens.json)与[界面文案](design/ui-copy.json)说明现行配置；新增双语文案在 `src/ui/i18n.ts`，不能反向覆盖产品目标。图片中的示例文字、会议日期、图表结构和比例不得硬编码成业务模板。

`ui-copy.json`目前被运行时代码直接引用，本轮未修改。其旧标题／输入源字段和`defaultLocale`不定义新交互；新文案、系统语言解析及正常／测试入口需由代码接入任务一起更新，避免只改字典就宣称行为完成。

当前方向为清晰的浅色协作办公界面、轻导航、稳定内容表面、按需来源与推演。具体色彩、层次、组件及八图描述见 style.md；本文不再要求复制旧第二张的构图。正文、图形和表格保持不透明。

## 2. 页面与组件地图

| 页面／组件 | 默认内容与主操作 | 退出与状态 |
|---|---|---|
| Meeting library | `Start meeting`、当前事件、历史事件；无事件时一个空状态 | 打开事件进入工作页；不自动采集 |
| Start meeting | 使用保存的音频设置直接开始；标题由Agent命名，语言自动解析 | 一次意图衔接创建与采集；首次设置／异常按需处理，取消不留音轨 |
| 首次音频初始化（尚无设计图） | 设备／权限状态、麦克风、电脑声音范围、显式测试、保存／取消 | 无会中顶栏；保存返回首页但不录音，测试结束释放设备 |
| Meeting workspace | 顶栏、生成工作区域、变化提示；有内容后出现轻提问入口，点击才展开；来源抽屉默认关闭 | 关闭窗口回桌面入口；不会停止正在运行的事件 |
| Desktop launcher | 可贴边的小图标、真实运行标记 | 悬停速览、左键打开、右键菜单、拖动定位 |
| Hover preview | 当前事件、一个焦点、最多三条变化；无会议时显示开始入口 | 离开联合区域后收起，点击打开工作页 |
| Context menu | 按事件状态提供操作 | Escape、外部点击或执行操作后关闭 |
| Source drawer | 原话、来源时间／身份状态、翻译入口、纠错入口 | 关闭返回原内容选择与滚动位置 |
| Settings | 音频设备与电脑声音、跟随系统／手动语言、视觉辅助、快捷键 | 全局音频偏好影响下次会议；应用到当前会议需明确操作；语言规则单独说明 |
| Ended event | 原工作页进入已结束状态，保留产物、决定、未决项和导出 | 重新打开不恢复采音 |

应用导航控制固定，工作内容结构不固定。不能把选定图中的对照表、依赖图和“Latest change”设计成每场会议必须填满的三张卡片。

## 3. 会中工作页的布局契约

### 顶栏

高度建议56–64 CSS px；从左到右为返回Meetings、事件名称、真实状态，右侧为设置／事件菜单、Pause／Resume与End meeting；语言只在设置中管理。空间不足时次要操作进入菜单，结束会议仍可清楚找到。

系统原生窗口控制保留，应用控件避开系统拖动区域。工作页是正常可移动／缩放窗口，默认不全屏、不强制置顶、不覆盖其他应用；只有轻量入口和临时浮层采用必要的置顶行为。

### 主工作区

参考视口1440×1024，桌面工作内容容器宽度在1100–1240px内校准，常规外边距32px；低宽度下缩到16–20px。主标题建议28px，可用空间大时不超过32px；不照抄概念图中过大的标题。

每次优先一个主问题及其生成内容，允许必要的辅助表达。旧话题通过历史／话题导航按需访问，无永久宽侧栏，无自动无限卡片墙。Agent可以生成组合内容，但应遵守同一视觉规范。

### 来源与详情

默认仅显示`View sources`入口，点击具体元素后打开对应来源。宽度足够时显示约360–420px抽屉；若展开后主内容不足600px，则改为侧面覆盖层，关闭后恢复主视图，不把图缩成小字。弹层不遮蔽不可恢复的当前输入。

### 提问入口

空会议、只有原话或处理占位时不显示提问框。出现有意义的Agent文字或图形后，在内容附近提供轻量入口，用户点击才展开；当前实现在生成内容及可信操作后提供“进一步讨论”，已有本地合成界面与桌面测试；最终用户场景质量仍需反馈。选中对象时附可移除的上下文标签，并关联产物／对象版本。用户发问后结果优先成为工作产物，不扩展为大段聊天流。Shift+Enter换行，Enter发送必须避开输入法组合状态。点击Send明确触发同样操作。

仅在用户展开后为提问区域预留空间，收起和空状态不保留空白底栏。区域不挡内容，实时更新保留焦点、草稿与上下文版本；模型处理中可继续阅读。普通会议不提供常驻文字来源注入，原话纠错保留在来源抽屉。

### 尺寸与缩放

至少设计验证1440×1024、1024×768、800×600工作窗口及200%文本缩放。宽度不足时先隐藏次要导航、折叠抽屉、换行控件；表格确需横向滚动时只滚表格区域。不得将整张图等比缩到文字不可读。

## 4. 桌面入口的状态机

展示状态：`collapsed | peek | workspace_open`；菜单状态`closed | open`独立。此前文档中的pinned统一解释为左键打开并持续显示工作页，不要求再点一次图钉才能保持打开，也不代表系统always-on-top。

| 输入／情境 | 必须行为 |
|---|---|
| 启动软件 | 显示入口，不自动录音；恢复事件内容不等于恢复采集 |
| 悬停入口 | 建议250ms后打开速览；不激活其他应用焦点 |
| 鼠标进入速览 | 保持显示；图标与浮层间的间隙也纳入关闭宽限 |
| 离开入口与速览整体区域 | 建议300ms后收起；返回区域取消关闭 |
| 左键点击入口／速览打开操作 | 打开当前事件工作页并保持；无当前事件则打开会议列表和开始入口 |
| 工作页已经打开时再点击 | 聚焦现有窗口，不创建副本、不重新录音 |
| 右键入口 | 打开菜单并关闭／抑制速览，直到菜单退出 |
| 拖动入口 | 位移超过建议6px后作为拖动，不触发点击；结束时贴近屏幕工作区边缘 |
| 关闭工作页 | 隐藏至入口，后台继续；不误发stop命令 |
| End meeting | 执行结束流程并立即显示Ending；音源释放后Ended，已接收内容仍整理则额外标Finishing notes |
| Quit且有进行中的事件 | 在可信确认中说明将结束采集并退出；取消则继续运行 |

时间、尺寸和拖动阈值是初始实施参数，不是已测试效果。鼠标离开隐藏的只是速览，不隐藏已打开的工作页。后台生成完成不自动弹出工作页。

图标建议44×44 DIP，位置记录相对display工作区，拔插显示器后钳制到可见边界。原生窗口命中区贴合实际入口／浮层，透明空白区域不能挡住其他软件。不能声称覆盖所有系统安全界面或独占全屏；目标系统需测试。

## 5. 右键菜单与快捷键

| 状态 | 菜单项 |
|---|---|
| 无活动事件 | Start meeting、Recent meetings |
| capturing | Open meeting、Pause capture、End meeting |
| paused／input_error | Open meeting、Resume capture（来源有效时）／Reconnect input、End meeting |
| draining | Open meeting；显示Ending，禁止重复开始／结束 |
| 通用组 | Keyboard shortcuts、Settings、Quit |

语言统一在Settings中管理，默认跟随系统；Interface language与Meeting output language分开，不用一个含糊选项同时改变两者。第一版一次只采集一场事件，进行中不再提供可暗中创建第二场的Start meeting。

键盘：Enter／Space打开已聚焦入口；Shift+F10或Context Menu键打开菜单；Escape关闭最上层菜单／速览／来源抽屉并恢复触发点焦点，不停止采集。菜单使用平台原生行为或等价方向键导航。

全局快捷键只优先提供Show/hide meeting，默认未分配，允许用户在Settings录入并测试冲突。开始、暂停与结束不默认注册全局键，避免意外采集／停采。快捷键注册失败必须可见，不吞系统按键。无需持续拦截所有键盘输入。

## 6. 视觉语言与材质

浅色为首版基准：纸白内容面、石墨文字、低饱和蓝强调、细分隔。正文建议15px／1.5行高，辅助文字不小于12px，字体优先系统无衬线并配中文回退；当前建议token和对比度校准见[style.md](design/style.md)；[JSON](design/tokens.json)维护当前运行参数。标题不用宣传页式超大字号。

材质仅用于桌面入口、速览、右键菜单与可选轻顶栏。正文／表格／图形底面保持不透明。可采用轻模糊＋接近不透明的底色＋细边界，避免折射、流光、亮边或随鼠标移动的效果。背景颜色不能改变事实状态的辨识。

苹果材质指南用于参考层级、克制和可读性；不是要求实现原生Liquid Glass或更换为SwiftUI。平台原生材质可用且可靠时使用，否则采用同样层级的实色底。模糊、跨窗口透明和真实桌面背景效果必须在目标桌面实测，不把CSS滤镜存在当作等价能力。[S13](sources.md)

响应Reduce transparency、Reduce motion或对应应用内开关；无法读取系统设置时提供明确开关。减少透明度后改用实色底，保留边界；减少动效后无非必要动画。失去窗口焦点或系统降低效果时不影响内容读取。

普通文字对背景对比度目标≥4.5:1，重要图形边界与控件≥3:1。参考图不提供这些保证，需实际验证。材料背景在亮／暗／复杂桌面上都要检查，不能只测白底。

当前已认可图组采用浅色；深色模式仍不是本轮必做范围，不能把其他探索稿当作已确认设计。

## 7. 状态、动效与更新

入口使用[独立图标与状态规范](design/launcher.md)：待命灰点、连接琥珀空心环、采集绿点、暂停琥珀暂停符号、异常红色叹号。会话服务错误覆盖旧采集快照；tooltip、无障碍名称与速览共用状态投影。capturing与Agent processing可同时存在，模型问题另行解释，不用点表示理解质量。

新内容只在受影响对象上短暂标识，轻变化过渡建议120–180ms；无常驻呼吸、旋转、弹跳。重复状态不重复通知。默认自动跟随；有文字选择时保护对应块，主动查看历史时保留所选版本并提供返回实时入口；旧内容若已失效必须立即标过期，不能等到用户应用布局才提示。

来源缺失、无数据、开始连接、转写有但理解等待、生成失败、试算基线过期、保存失败均有独立文案。不能为填满空白生成假图、假参与者、假百分比。

## 8. Agent生成内容如何继承外观

宿主向生成器提供`visualProfileId`、目标尺寸、字体尺度、可用颜色／间距／图形语法和locale。Agent可决定内容结构、图形数量与布局，但不能修改顶栏、采集状态、设置或复制一套新的应用外壳。

SVG中使用token映射的颜色与字号；HTML／chart由隔离宿主注入同一视觉配置。代码净化后仍检查是否绕过字号／对比／边界约束。语义需要多系列颜色时提供有限、可区分的序列色，不强制所有数据同色。

生成器不需要逐次复制磨砂CSS。材质由可信宿主提供；产物以可读内容为主。改变产物语言时允许换行重排，但保持业务对象、来源及试算语义不变。

## 9. 实施验收

先按[style.md](design/style.md)及本规范实现可信壳、入口状态和双语文案，再连接真实Agent生成内容；开发占位不能计作生成完成。与本轮 8 图比较时，按 style.md 的行为修正和参数校准验收，不能沿用旧第二张作为当前唯一目标。首次初始化和异常状态尚无完整设计图，需要实际界面补验。

通过标准见[AC-28–AC-35](acceptance-criteria.md)。需要工作页、速览、菜单、设置、结束与错误状态的实际验证；不能只凭一张漂亮首页宣布前端完成。


## 10. 页面与现有源码的接入导航

以下为当前前端接入位置；对应验证以[本轮记录](sessions/2026-09-11-frontend-refresh.md)为准：

| 对象 | 当前接入位置 | 设计依据 |
|---|---|---|
| 首页、会中顶栏、空会议、结束回看、桌面入口／速览 | [main.tsx](../src/ui/main.tsx) | style.md 图 1、2、5、7、8；启动软件只显示轻入口 |
| 设置、来源抽屉、试算／决定操作 | [components.tsx](../src/ui/components.tsx) | 图 3、6、首次初始化和真实性规则 |
| 阅读／版本／草稿保护 | [live.tsx](../src/ui/live.tsx) 与 main.tsx | 图 4、更新与过期状态 |
| 动态产物及关系图 | [ArtifactView.tsx](../src/renderers/ArtifactView.tsx)、[RelationshipGraph.tsx](../src/renderers/RelationshipGraph.tsx) | 通用动态表达，不能写死试点会议两栏模板 |
| 样式与语言 | [style.css](../src/ui/style.css)、[i18n.ts](../src/ui/i18n.ts)、[ui-copy.json](design/ui-copy.json) | 当前风格、系统语言和中英长文案；需与生成器视觉配置统一迁移 |
| 真实窗口与菜单行为 | [desktop/main.ts](../src/desktop/main.ts) | 左键持续打开、悬停速览、关闭不停止；不以静态图证明行为完成 |

上述代码已接入新视觉；`src/ui/theme.ts`统一颜色变量，后台 preflight 与生成 HTML 同步接入。首页搜索／筛选、动态目录、来源目标、首次音频设置和完整设置的分组见 style.md 第 11 节。后续修改仍需保护工作树并分别验证。


## 11. 当前实现与目标差距

表格基于`a587a92`前端基线及其[验证](../tests/results/frontend-refresh-validation.md)，对象推演／任务与澄清已按2026-09-12工作树及[工作流验证](../tests/results/agent-workflow-validation.md)更新；日常安装包未随本轮替换。前文含目标契约，本节区分实际承接，不以代码现状取消尚未实现的目标。

| 对象 | 当前实现 | 仍需区分的边界 |
|---|---|---|
| 首页 | 开始／打开、设备摘要、全部／进行中／已结束筛选、标题搜索、清除筛选和历史列表 | 未新增列表行菜单、共享空间或协作成员 |
| 工作内容 | 文档式表面；会议内容／来源／版本工具栏；可收起的动态块目录、导出和元信息 | 目录只定位实际生成块，不是固定业务步骤；导出为JSON，不是Markdown／PDF |
| 来源 | 块／行／图关系关联来源，目标提示、原话、身份未确认、翻译、来源纠错；同一会议关闭抽屉保留草稿及返回焦点 | 尚无单独“纠正Agent理解”按钮；草稿仅在当前UI会话保留，不承诺跨切换会议／重启持久化 |
| 推演 | 有内容且会议活动时按需展开；上下文标签绑定artifactId＋revision及可选objectRefs，可移除；个人结果与会议事实分开 | 现已可在提问入口选择产物关联对象ID／revision，并在发送时冻结基线；结束回看暂无新的提问入口 |
| 实时更新 | 普通块自动更新；文字选择保护对应块，历史视图与个人试算保留基线；显示过期状态 | 尚无所有重排统一排队并由用户点击应用的通用机制；原设计要求仍待进一步实现 |
| 首次音频设置 | 点击开始发现配置缺失／设备不可用时进入仅音频设置，保存后回首页；不自动录音 | 尚未提供独立“测试麦克风”按钮；电脑声音失败后的明确仅麦克风继续选项未单独实现 |
| 完整设置 | 音频／语言／显示分组导航；常规窗口局部滚动和保存区；有效高度≤500px时整页滚动 | 分组是页面内定位，不是切换到另一份设置；界面语言选择后独立即时保存，其他设置草稿保留；本场输出语言选择立即生效 |
| 尺寸与关系图 | 来源宽400px；视口≥1440px时主区让位，否则覆盖；≤620px时关系转为可追溯的文字链 | 使用明确断点，尚未动态测量“主区不足600px”决定覆盖；长图和真实内容组合仍需实测 |
| 外观与载体 | 全局collaborative-light-v2颜色、系统字体与lucide图标；隔离HTML与preflight同源主题，生成器接收视觉指导 | JSON颜色可共享，排版／间距仍由CSS和组件实现；SVG主题符合度不是自动审美保证；生成JS禁用 |
| 桌面与交付 | 生成图标＋透明无白边入口、五态采集提示、左键工作页、隐藏不停止；macOS原路径应用已更新，历史保留 | 新版Windows真机、完整屏幕阅读器和真实模型／音频仍未验收；本次未新增签名或分发能力 |

主要定位：`src/ui/main.tsx`（首页、目录、推演及错误路由）、`components.tsx`（设置和来源）、`live.tsx`（保护逻辑）、`src/renderers/ArtifactView.tsx`／`RelationshipGraph.tsx`（载体）、`src/ui/theme.ts`／`src/desktop/preflight.ts`（主题与预检）。


## Agent 可靠性入口补充

会议产物下可展开“条件与承诺依据”，显示当前对象的条件、原话承诺、负责人／时间及待核对状态。结束后出现“会议结束核对”，按需展开未决事项和汇总会议记录，可回到来源；历史正文不被当前依据覆盖。具体规则与 ready／needs_review／pending 边界见[可靠性规范](agent-reliability.md)。

## Agent工作流交互

新增[WorkflowPanel](../src/ui/WorkflowPanel.tsx)展示个人请求状态、取消、基线变化、重基提问，以及带来源的澄清回答／取消／自然消解。澄清回答只进入个人分支，等待回答不暂停会议。输入缺口可查。未发送草稿仅在当前UI存活；实际合成检查及未测边界见[工作流验证](../tests/results/agent-workflow-validation.md)。


## 界面交互细节（2026-09-12）

品牌名、导航、设置标题和辅助说明等固定外壳文字禁止拖选；会议产物、原话、译文、核对内容、错误诊断与输入框保留选择复制。按钮／表单保留键盘焦点环，Tab只遍历弹窗内可见可用控件，不进入折叠诊断。禁用按钮不显示可点击的悬停反馈，图标点击落在所属按钮。界面语言即时独立保存；失败在设置中明确提示并允许重试，不丢其他草稿。实际范围见[本轮交接](sessions/2026-09-12-interface-polish.md)。
