# 决策与确认记录

COL04（2026-09-12，用户进一步纠正）：结合上下文与会议业务关键词，已填充的组件应自动冒出在固定区域，以内容缩略卡展示；悬停或点击后看完整卡片和审核页，host只审核并分发。COL03的静默分析、完整生成继续成立；取代仅在悬浮球显示数量、需用户自行找组件的主路径。收集态有具体内容后即可显示更新，不把空白编辑表当生成结果。实现见[自动区验证](../tests/results/component-dock-validation.md)。

COL03（2026-09-12，用户纠正）：正常流程必须是静默记录／分析自然对话 → 推断协作需求 → Agent准备完整默认组件 → 悬浮球提示 → host只读审核并一键分发。不得要求任何一句话对Agent下指令，也不得要求host填表、逐项组装选项或配置默认规则。所有现有及第二批组件一致。编辑仅为可选纠错；未知必需事实以具体澄清保留，不能编造。此项替代首版运行说明中的手动启用、初始编辑及手选所有依据作为正常前提，也替代指令式v1台词作为正常验收证据。实际修正与验证见[本轮记录](sessions/2026-09-12-agent-component-review.md)。

方向基线：`meeting-event-generative-v3`｜2026-09-11。证据来源：用户在本任务的连续产品讨论及最后的独立目录创建请求。本文是对这些确认的转述，不伪造逐字引文或外部会议纪要。

## Demo 展示优先级（2026-09-12）

DEMO01：用户明确要求对结构和可视化更敏感，面向评委展示优先于保守不变的画面，并缩短持续发言到表达的等待、体现Agent编辑。采用主动语义选型、同轮生成普通图、真实流式草稿和局部更新；替代文字／表格优先和原话核对冻结整份产物的旧取舍。来源、身份、未知和会议确认边界仍有效，不能用预写答案伪造能力。详见[实时表达规范](demo-live-visuals.md)。具体2.4秒分段、250ms合并与动画时长为工程选择，不是用户承诺或真实效果指标。

## 如何读取本记录

COL01（2026-09-12，用户要求的设计范围）：设计首版投票／分工／冲突／决定确认，包含意图、预设schema与样式、发起者发放、本地模拟参与者和完整LangGraph接入。它扩展此前“共享可后续”的范围，并仅对这些业务交互采用固定组件；未取消通用生成式表达。见[完整设计](collaboration-v1-design.md)。该设计阶段当时未实施，授权不包含产品编码、Git提交或外部发送；后续实施与 PR #3 合入已由独立任务完成，见[修复交付](sessions/2026-09-12-pr3-fix-merge.md)和[实际差距](collaboration-v1-runtime.md)。保留原阶段授权，不将其误读为当前仍未开发。

COL02（2026-09-12，第二阶段规划）：用户确认“先补关键闭环，再按上述顺序新增组件”，其中顺序为议程、信息收集、方案对比、风险与问题清单。保留悬浮窗展示、悬浮球提示和发起者发放。已形成[设计](collaboration-phase2-design.md)及[实施计划](collaboration-phase2-plan.md)；具体schema和工程拆分为规划提案，尚未实施。本轮确认优先级，不代表第二批已经实现。

本文件按阶段保留决定；每阶段确认有作用范围，不代表今天的任务授权。产品现行要求见[产品定义](product-definition.md)和[状态页](status.md)。C09／C14 已随 C15 结束；C10 的固定英文默认被 UX04 取代，C11 的旧第二张参考被 VIS01 取代；C16 确认双端目标。新的替代关系应更新现行正文，并在此明确记录。

## 产品确认历史与仍有效的目标

| ID | 内容 | 本轮依据 |
|---|---|---|
| C01 | 持续理解自然会议，按需查看、追溯、纠正和主动推演 | 用户认可前述完整使用链路 |
| C02 | 线上和线下会议均在范围，Zoom只是输入之一 | 用户明确补充线下多人会议 |
| C03 | 每场会议是一件独立事件；整体按桌面应用与轻量入口设计 | 用户提出事件区别于对话框，并认可应用层次 |
| C04 | Agent自主生成表达结构、图形和交互内容 | 用户指出只填预设前端视图不符合意图，并确认纠正 |
| C05 | 工作级、简短、清楚、能可视化逻辑 | 用户明确拒绝冗长复述和比会议更长的Agent输出 |
| C06 | 五类用户问题、承载组合、判断与更新逻辑作为设计依据 | 用户最后一轮明确认可所提出的这些内容 |
| C07 | 人物信息按可靠性提供；基础会议理解不依赖完整实名识别 | 用户提出识别不准时取消该能力的顾虑，后认可其他产品内容；采用可靠／分组／未知分层，不承诺准确率 |
| C08 | 新建独立代码与MD目录，为后续单独GitHub仓库准备；原有材料保留 | 用户本次明确创建请求 |
| C09 | 本轮先出实现文档，真正开发由后续一轮继续 | 用户明确阶段顺序 |
| C10 | 历史确认：产品英文默认、简体中文可选；固定默认部分已由下文 UX04 取代，会议中英支持和原话／输出分离仍有效 | 用户确认上一轮语言方案；本轮明确修正默认策略 |
| C11 | 历史视觉选择：采用当时实际显示的第二张；现被 VIS01 取代 | 用户明确选择；原图已保存到docs/design/assets |
| C12 | 参考苹果的细腻、克制材质，不抢眼、不干扰工作 | 用户本轮补充；具体参数为工程细化，非效果保证 |
| C13 | 运行时桌面入口：悬停速览、离开收起、左键打开工作页、右键快捷操作 | 用户上一轮提出并认可交互方案 |
| C14 | 当前任务只负责出完文档，另一个工程实现 | 用户本轮再次明确，不启动产品开发 |

C07确认的是允许可靠性边界，不是“说话人识别已测试不行”或“永远不做人物功能”。具体阈值、技术路线与可用覆盖率仍待测试。

## 被修正的旧理解

| 旧理解 | 当前解释 |
|---|---|
| 预算筹备会作为核心业务 | 预算只是一个可验证推演例子 |
| 所有内容都需主持人发布，其他人才能看见 | 普通内容默认个人按需查看；COL01新增可选协作组件，正式发放由发起者控制 |
| 所有场景必须准确实名后才可用 | 身份分层，未知时仍维护观点与分歧 |
| 所有工作表达都固定为模板填数 | 通用表达仍由Agent规划；COL01新增四类带业务交互的固定schema组件，与普通表达并存 |
| 任意生成全部后置 | 通用应用生成可后置；会议工作表达生成必须实现 |
| 只有Zoom或电脑远程会议 | 线下输入同样是产品需求 |

外部技术设计v0.1仅为参照，其版本／去重／来源／试算机制可借鉴；预算领域模型、主持人共享舞台和未核验赛程不能成为当前依据。本目录未复制外部文件，也不依赖它才能实现。

## 设计阶段工程建议（历史）

以下为采用前建议；已采用部分见 E01–E03／ADR-003，最新改造见状态与对应 session，不再据此判断未选型。

- TypeScript＋Electron＋React，可信本机会话服务，SQLite持久化；见[ADR-001](adr/001-local-session-service.md)。
- 表达描述、图语言、SVG、图表规范与受控HTML组合；见[ADR-002](adr/002-generative-expression-runtime.md)。
- 初期顺序工作流与有界修复，不强制LangGraph或多Agent。
- 先验证一套实际演示桌面系统；未指定必须Windows或必须macOS。
- 线下单采集设备、个人本地会话服务优先；多人共享需要额外服务，不默认随桌面首版实现。
- 性能阈值、输入与产物大小上限、保留策略初值均属可测试参数，不能写成实测效果。

## 尚需实际验证

目标演示机与系统版本、系统采音、平台账号权限、模型与语言质量、声音分组和姓名映射、HTML隔离与性能、真实时延、使用成本、团队分工、官方比赛规则及提交要求。

文档完成不等于这些事项完成；不从建议的开发阶段推导真实工时。后续采用或更换技术选型时追加依据和测试结果，不回改用户已确认的产品目标。

## 前端交接依据

[前端规范](frontend-spec.md)、[语言规范](language-spec.md)和[设计决策](design/decision.md)将本轮确认转为可执行约束。选定图用于构图和风格，不是固定业务模板；具体文字、图形和比较维度仍由Agent生成。中文界面范围为简体中文、中文语音首版验证普通话；不额外承诺方言能力。

## 2026-09-11 本地实施确认与工程落地

- C15（用户确认）：接手并实际开发、验证首版本地产品；C09/C14 的文档阶段约束已结束。该开发请求当时不含 Git 提交／推送或部署；后续首版交付授权仅属于原任务，不对新 session 生效。
- C16（用户确认）：macOS 与 Windows 同时为目标，架构必须适配当前需求并保持清楚的边界。
- C17（用户确认）：暂时没有模型／语音凭证，先完成不受影响部分。
- E01（工程采用）：Electron＋React＋TypeScript，单写会话 utility process，SQLite；真实适配器与领域隔离。
- E02（工程取舍）：首版禁用生成 JS，以被动 HTML/SVG＋可信参数控件承接交互；5 秒 PCM 音频切片，语音实时流可替换。
- E03（证据边界）：真实桌面与本地测试服务已执行；真实模型、多人会议音频、Windows 真机仍待验证。详情见 [ADR-003](adr/003-cross-platform-first-version.md) 和 [测试记录](../tests/results/validation.md)。

## 2026-09-11 会议入口体验修正

依据：用户查看实际前端后反馈；本轮用户明确选择先改规范、由正在运行的工程任务统一接入代码。以下保留当时的新确认，覆盖前面冲突要求；确认当时不代表实现，后续已接入的范围见VIS05–VIS08与当前前端规范。

| ID | 用户确认 |
|---|---|
| UX01 | Start meeting 不应每场询问标题、输入源和输出语言；正常流程应直接开始 |
| UX02 | 会议标题由 Agent 根据本场内容生成，不能要求开会前填写 |
| UX03 | 麦克风在初始化／设置中选择和匹配；Text input 和合成回放退出正常会议入口，电脑声音与麦克风用清楚的采集范围解释 |
| UX04 | 默认匹配系统：中文／英文对应，其他语言用英文；设置保留覆盖，页面不常驻语言切换 |
| UX05 | 空会议没有 Ask about this meeting 输入框；有 Agent 内容后才考虑轻量提问入口，具体呈现尚未最终确认 |
| UX06 | 前端需要清楚的视觉层次与更精细的排版；旧图不要求照搬常驻底栏 |
| UX07 | 本轮只修订规范与衔接逻辑，代码由正在运行的任务统一接入 |

实施细化与用户确认分开记录在[会议入口规范](meeting-entry-spec.md)：设备默认／恢复、一次启动协调、幂等、标题版本、语言迁移、渐进入口和视觉验收。前端规范、语言规范、产品定义与验收条款已同步；现有代码和运行时字典不能作为否定新规范的依据。

## 2026-09-11 跨 session 文档接续

- DOC01（用户确认）：每个新 session 都应从 AGENTS.md 找到现行认知、进度和相关任务记录；不依赖原聊天才能接手。
- DOC02（用户确认）：每次迭代同步受影响的项目文件、MD 与具体 session 描述，说明做了什么、验证、未完项与下一步；不能只在聊天中汇报。
- 实施方式：两层入口、按任务路由、单一状态入口、多个并行 session 记录及文档检查。用户确认目标；具体文件组织为本轮维护方案。详见[交接流程](sessions/README.md)。


## 连续 Agent 实施确认

用户在任务 `01a090b1-080f-7b43-b3ee-1b78477461c2` 明确确认并授权实现：持续输入、页面默认自动演变、增量理解、长会记忆与费用控制。普通阅读不冻结工作页，局部保护文字选择与个人草稿。工程采用 [ADR-004](adr/004-live-agent-pipeline.md)，保留首版后台与隔离基础。本任务同时收到会议入口任务的明确交接并接入其已确认规范；真实供应商和双端设备质量仍待验收。


## 2026-09-11 新视觉认可与规范落地

- VIS01（用户确认）：不向生成端提供旧参考图，自由探索后认可本轮 8 张图的整体风格，认为清晰、具有现代协作办公产品的感觉。这替代 C11 的旧第二张具体视觉基准，保留内容优先与低打扰目标。
- VIS02（用户确认）：请求生成端输出 style.md，并把风格和页面描述同步进项目 MD，供后续前端迭代；不代表本轮授权修改产品代码或提交／发布。
- VIS03（工程整理）：[style.md](design/style.md)为对生成端可见正文的本地整合修订版。原附件下载被 Chrome 组织策略拦截，未冒充原始文件副本；已校准启动入口、音频降级、设置生效、受控渲染与可读性色对。
- VIS04（证据边界）：8 图和原规范已在 Chrome 读取；首次初始化尚无图。建议 token、补充行为、静态图和局部对比度计算不能代替真实交互／双端验收。原文件与图组可在 style.md 来源会话审阅。


## VIS05–VIS08：前端实现、布局细化与本地更新

- VIS05（用户授权）：按新style.md大幅改善现有前端与动态内容呈现；不恢复固定业务模板。
- VIS06（用户确认与方向）：实现过程中认可整体视觉，要求参考飞书云文档、文档正文、会议及多个模块，继续打磨工作流与细节。具体采纳见[style.md第11节](design/style.md#11-飞书布局参考后的本地细化)。
- VIS07（交付事实）：用户后续明确授权提交／推送，前端代码为 `a587a92`，交付记录为 `38c70b1`；随后授权替换本地.app，2026-09-12完成原路径更新和窗口核对，见[记录](sessions/2026-09-11-frontend-refresh.md)。旧VIS02“仅整理文档”的授权边界只适用于该早期阶段。
- VIS08（本轮授权）：用户要求同步本地与仓库全部相关描述；仅修文档与当前／历史边界，未因此新增产品功能、测试通过、提交或推送授权。


## VIS09：桌面入口图标与五态

2026-09-12用户认为旧小图标简陋、有白边，要求图像生成改善标识，并明确右上角状态。采用独立生成的对话环形入口素材，透明原生窗口、无白边；待命／连接／采集／暂停／异常五种可见状态由可信采集和服务状态投影，模型处理质量单独说明。细则见[图标规范](design/launcher.md)，结果见[本轮记录](sessions/2026-09-12-launcher-icon.md)。不扩展为Dock／托盘图标统一或真实录音授权。


## 2026-09-12 Agent 可靠性优化

- 用户确认按①不混淆事实、②不漏掉变化、③不带着遗漏结束的顺序优化。随后明确授权实现、验证，通过后提交并推送 GitHub。该外部操作授权仅属于本轮任务。
- 工程落地：结构化证据和条件、上下文隔离、依赖失效标记与保留历史、确定性结束核对。具体限制见[规范](agent-reliability.md)，实施与验证见[交接](sessions/2026-09-12-agent-reliability.md)。


## 2026-09-12 研究后的业务复核与实施授权

用户要求在研究基础上明确业务对应和具体实现，完成文档后新建一个Session实施，并确保交接与验证质量。本轮工程选择局部LangGraph工作流、可信提交／恢复、主动本会议检索、独立个人推演、低打扰澄清和表达修复；具体范围见[任务书](agent-workflow-implementation.md)，验收见[场景](agent-workflow-acceptance.md)。上述工程选择已由实施任务接入本地工作树，采用依据与边界见[ADR-005](adr/005-bounded-agent-workflows.md)及[运行时说明](agent-workflow-runtime.md)；真实效果尚未验收。原实施请求未授权真实采音、付费供应商调用或Git交付；用户随后明确授权高质量验收通过后提交并推送GitHub，见[验收交付记录](sessions/2026-09-12-agent-workflow-acceptance-push.md)。真实采音、付费模型和部署仍未授权。


## 2026-09-12 界面语言与交互细节

用户明确要求界面语言选择即生效，替代语言规范中等待“保存设置”的旧行为；跟随系统读取首选系统语言，修复应用locale与系统语言不一致。固定外壳文字禁止拖选，会议工作内容与输入保留复制；两端共用实现并同步本地Mac／Windows包。范围与验证见[本轮记录](sessions/2026-09-12-interface-polish.md)，Windows真机结果不得由包一致性代替。

## 2026-09-12 会议候选提醒

用户在当前任务逐步确认并授权实现：统一“你可能正在开会／点击开始记录”，不提具体会议软件；球可见时气泡、隐藏时系统通知，点击开始、忽略无动作，中英文齐全。本轮只做可信检测信号后的提醒链路，检测算法独立。8秒气泡、悬停暂停、一次提醒、过期拒绝及静默开始是已接受的实施细化；提醒入口取代旧的“一切开始都自动打开工作页”行为，首页开始不变。详见[专项规范](meeting-reminder-spec.md)。


## 2026-09-12 设置即时生效

用户明确要求设置实时变更，尤其悬浮球开关点击立即看到效果。本轮以字段补丁自动保存替代“除界面语言外整份Save”的旧要求；控件／选择即提交，快捷键输完提交，完成按钮不再承担普通保存。默认音频可即时保存，但当前录音来源保持明确应用操作。此条替代会议提醒设置需Save生效的旧描述。

## 2026-09-12：会中正文与窄原话边注

用户确认第一张方向并授权实现。宽屏正文在左、原话360–380px，保留列宽避免开关时横向跳动；窄窗覆盖。来源编号绑定ID与revision；反馈理解为待发送个人草稿。没有可靠字符范围时不猜高亮句。专项规范见[meeting-editorial](design/meeting-editorial.md)，不将概念图的比较表固化成业务模板。


## 2026-09-12 PR #3 内容与本地认知对齐

- DOC03（本轮用户确认）：具体核对最新 PR #3 的协作流程和 Live Transcribe，更新本地项目管理 MD。本轮授权为文档对齐，不承接其他任务的代码修复、提交或合并。
- 范围依据：PR 内设计／实施记录记载四类可选协作组件、独立悬浮窗、球就绪提示、M/C/R 工作流和本地模拟。它补充此前“共享可后置”的范围，仅对四类业务操作采用固定 schema；普通表达仍由 Agent 自主生成，个人按需查看无需发起者发放。当时发现原分支 COL01 和产品定义的“尚未实现”与运行报告冲突；当前正文已按 PR #3 合入及后续分支整合校准，历史对照保留原审查范围。
- 用户在[修复任务](sessions/2026-09-12-pr3-fix-merge.md)已要求保留 GPT Live Transcribe，修好后合并。由该任务实现异步授权队列、输入缺口门槛和新版回应重算；这里记录既有方向，不将它扩展为本轮外部操作权限。
- 当前／历史替代：E02 的约 5 秒文件切片是首版路线，PR 将默认输入改为 Live，显式文件模型仍可使用旧路径。原始凭证缺失是历史事实；Windows 合成实调、此 Mac 配置、真实会议和最终双端包验证分别核对。

具体差异、精确提交来源、三项反例与交付门槛见[对照文档](pr3-doc-alignment.md)。已采用方向、分支实现、本地主分支整合及真实效果验收不得互相替代。


## 2026-09-12 跨会议主题文件夹与综合纪要

用户要求把同一主题的多场会议放进一个文件夹并出一份综合纪要，本轮确认六项：

| # | 决定 |
|---|---|
| 1 | 内容重点：决策与未决项追踪 |
| 2 | 追溯：每条信息标到会议 + 原话 |
| 3 | 分歧：并列保留，不裁判 |
| 4 | 生成：手动点一次，生成不可变快照 |
| 5 | 失效：标记过期，手动重生 |
| 6 | 文件夹：完整增删改查 |

**替代关系：** [产品定义 §7](product-definition.md) 原把"自动跨会记忆"整体列为可后置。本决定替代其中"用户显式声明的分组"部分：关联由用户声明，系统不推断相关性。**自动跨会记忆本身仍可后置**，正文已同步改写，替代项在此明确记录。

工程取舍与边界见 [ADR-006](adr/006-cross-meeting-collections.md)；范围与验证见[本轮记录](sessions/2026-09-12-meeting-collections.md)。真实模型语义质量、模型是否真的并列呈现分歧，均未验收。


## 2026-09-12 悬浮球直接速览实时画板

用户明确要求会议在后台进行、主工作页关闭时，悬停悬浮球直接看到 Agent 画板及实时更新，移开收起，拖动不误触。此决定细化C13，并替代前端规范／图7中的“一个焦点、最多三条变化”的速览内容限制；左键持续打开工作页、联合区域宽限和不抢焦点保持有效。实施使用同一会议产物渲染器与快照，不额外调用模型；来源与动作转入工作页，动作只预填草稿。默认尺寸、延迟为工程细化，非用户逐项指定。验收见[本轮记录](sessions/2026-09-12-hover-canvas.md)。
