# 当前首页细节精修 · 核验

final result: passed

范围：独立HTML视觉预览完成，待用户评审。只证明保留当前结构、页面可渲染和局部交互；不代替用户审美认可。v1/v2已被用户否定。

## 基线与对照方法

- 当前应用来源：[本轮直接捕获](qa/current-home.png)，1180×808 CSS px、2360×1616 PNG。隔离空数据目录，禁用真实模型和开发输入，无采音。缺服务提示是隔离运行状态，不是对用户日常凭证的判断。
- [实际DOM和CSS导出](qa/home-source.json)＋[设置弹窗导出](qa/settings-source.json)。HTML直接使用此DOM和完整CSS，未重新设计首页结构和文案。
- [当前界面](qa/current.png)与[精修效果](qa/polished.png)在同一1180×808视口、2x密度、同一页面状态下成对截图，已同次输出目视比较。JPEG均为等比例1x查看副本，无裁切。
- 程序核对：切换当前／精修前后 `.app-shell.innerHTML` 完全相同，仅根节点data-polish改变样式。预览控制条在产品外壳之外；实际截图对照中排除该条。
- 飞书参考：[官方常见页面设计原则](https://ae.feishu.cn/hc/zh-CN/articles/768591170201)，已用独立浏览器完整读取文字，核对间距、文字、圆角、阴影及图标原则；[页面截图](qa/feishu-design-principles.jpg)。本轮保留既有上下布局，没有再引入飞书侧栏或模板。

## 五项视觉检查

1. 字体：原系统字体与原文案保留，标题32px，说明由17px细化为15px，字色重新分级；辅助文字经二次加深。既有顶部品牌、欢迎语、开始操作和最近会议顺序不变。
2. 间距：内容顶部48→40，欢迎区44/48→40/44，开始与设备保持并排并用轻分隔；组间距统一收紧。1180宽最近区域底部从约712.5变为649px。
3. 色彩／边界：冷白调整为更中性的浅灰；内容不透明；主区域圆角18→12、按钮10→7、列表14→9。服务提醒减轻填色并保留可见边界与原文，空态虚线改连续细边。
4. 素材：首页与设置图标均来自真实捕获的现有Lucide SVG；列表演示同样来自项目图标库。无新增插画、侧栏、示例卡片或品牌重绘。
5. 内容：默认0会议，与当前捕获一致；额外“服务就绪／已有会议”仅为预览状态，已有列表使用原版结构与2条明确标记合成记录，不替换正常首页。

## 修正记录

- 初版800×600底部最近会议约606.9px，超出预览条顶部562px。仅在短窗口收紧原区域内部间距，最终551.9px，全部内容在预览条上方，未改阅读顺序。
- 初版辅助文字过淡，已加深主说明、脚注、空态说明。未做全页面WCAG认证声明。
- 明确 `[hidden]` 样式，防止预览恢复控件被自身grid显示规则覆盖。
- 无剩余阻塞该视觉预览的P0/P1/P2。是否继续采用仍由用户评审。

## 交互及尺寸

[12项检查](qa/checks.json)通过，页面控制台未捕获错误：当前文案／结构、同DOM样式切换、短窗不遮挡、设置与本地音源摘要、原有过滤和清除、模拟关闭／恢复、200%无横向溢出。设置是捕获原表单的预览，不注册系统快捷键、不显示／隐藏真实悬浮球、不改变真实语言和模型配置。

已检查[1440](qa/polished-wide.png)、[800×600](qa/polished-small.png)、[390×844](qa/polished-mobile.png)、[设置](qa/settings.png)、[已有会议](qa/history-polished.png)、[200%](qa/zoom-200.png)。截图DOM来自独立Electron，不依赖被工作区符号链接阻断的CUA／view_image。独立进程未操作日常Meeting Agent数据。

正式Windows/macOS代码与包未变；Windows真机、真实模型／音频和完整无障碍不在本轮验证范围。复现：在本目录运行README的HTTP服务，再从仓库根用 `node_modules/.bin/electron docs/design/prototypes/home-v3/qa/verify.cjs`。
