# 第四版：首页客户端视觉核验

final result: passed

用户选择“像飞书客户端：清爽、有层次，控件细节明显更讲究”。本版为独立预览完成，视觉仍待用户确认；前三版未获认可。

## 基线和可见变化

基于5878c20及现有工作树当前构建重新捕获的[实际首页](qa/current-home.png)，保留顶栏、欢迎区、开始会议、收听范围、提示与最近会议的上下结构。当前视图直接恢复捕获的HTML；优化视图加入欢迎内容／操作条分层、麦克风下拉、电脑声音开关及原音频设置入口。没有引入侧栏或大示例产物卡。

[原界面](qa/original.png)与[优化界面](qa/client-standard.png)均1180×808 CSS px、2x PNG；同次输出并列检查。同名JPEG为等比例1x副本。原文标题保留，辅助说明改为点击开始才会录音；电脑声音收听电脑输出，含线上参会者及可能的其他应用声音，已在开关title中解释。

## 视觉与交互

- 字体：系统字体，36px主标题、15px说明；控件的标签与值分层。窄屏标题按逗号自然分行，避免“楚。”孤行。
- 表面：浅冷色欢迎区／白色操作条，最近会议统一为带分隔表头的白色区域。页面结构与功能不变，新增容器只组织现有内容。
- 控件：麦克风选择框有展开／选中／焦点，电脑声音有明确开关与文字状态；切换原版保留预览值。设置只在本页生效。
- 图标：现有Lucide；未使用额外插画或已检查的桌面图标充当首页装饰。
- 响应式：[1440](qa/client-wide.png)、[800×600](qa/client-small.png)、[390×844](qa/client-mobile.png)、200%缩放已捕获。800窗口最近区域底部557.4px、预览条562px，无遮挡；390无横向溢出。
- [下拉菜单](qa/microphone-menu.png)、[选中态](qa/audio-selected.png)、[既有会议列表](qa/history.png)已检查。初次截图改变窗口宽度触发菜单关闭，修正检查顺序后完成；不是界面点选失效。

[11项检查](qa/checks.json)通过，页面控制台无错误。修正了辅助文字过淡、悬停优先级、窄屏标题断行和最近会议标题对齐。无剩余阻塞本预览的P0/P1/P2；不声明用户审美认可、完整无障碍或Windows真机通过。

验证使用独立Electron和临时userData，无真实采音／模型调用。正式Windows／macOS源码、应用与包未变，重打包不适用。底部示例状态与预览控制不属于正式UI。
