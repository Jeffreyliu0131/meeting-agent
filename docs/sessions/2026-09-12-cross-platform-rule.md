# Windows 与 macOS 每轮同步规则

记录类型：本任务执行记录。
状态：完成（仅文档）。更新时间：2026-09-12 10:51（Asia/Singapore）。

## 目标与授权

用户明确要求在 Agent 每次必读的位置写清：通过 Electron 同步开发 Windows 与 macOS，每次开发和迭代都必须同步两端。本轮仅修改协作入口、交接流程／模板与状态记录；不修改产品代码、不打包、不提交／推送。

## 接手基线

HEAD `37a2249`。已读两层 AGENTS、状态、session 索引、界面打磨与双端包同步记录、ADR-003、package.json 和文档检查脚本。已有状态／索引、桌面 main、UI components／i18n／main／style、桌面测试改动，以及飞书交接与界面打磨两份未跟踪记录。保留这些内容，不代其他任务宣布完成。

## 实际变化与依据

原入口仅规定两端同为首版目标、分别验收；缺少每轮实现与本地包同步的明确完成条件。已在工作区根 AGENTS 与[代码库 AGENTS](../../AGENTS.md)的前部加入每轮必读要求：双端影响、共用实现与平台适配、同基线本地包更新、分端验证及缺口判定；在[交接流程](README.md)与[模板](template.md)固定两端状态，更新[当前认知](../status.md)。

## 验证与未验证

2026-09-12 10:51（Asia/Singapore），本地 macOS、HEAD `37a2249` 加本轮文档工作树：`python3 scripts/check-docs.py` 通过（65 篇 MD／18 条 session），`git diff --check` 通过。已读回两层入口及共享状态差异，保留其他任务内容。检查只证明文档结构与链接，不证明产品双端同步。

### Windows 与 macOS 同步状态（每轮必填）

| 平台 | 本轮影响与实现状态 | 本地包位置／架构／源码基线及一致性证据 | 实际验证与未验证 | 缺口与下一步 |
|---|---|---|---|---|
| Windows | 不适用：本轮只修改文档，未改变运行产物 | 不适用：未改变运行产物 | 未运行产品测试／打包；文档检查通过 | 本轮无产品交付缺口；既有真机待验结论不变 |
| macOS | 不适用：本轮只修改文档，未改变运行产物 | 不适用：未改变运行产物 | 未运行产品测试／打包；文档检查通过 | 本轮无产品交付缺口 |

## 未完项与下一步

本轮规则沉淀完成。后续开发及续写中的任务按新规则填写两端状态；界面打磨任务的实际同步与验证仍由原任务维护，本轮不替其验收。

## 文件同步与交付

两层 AGENTS、session 流程／模板、状态、本记录与索引已同步，文档检查及差异格式检查通过。仅本地文档修改，未提交／推送；根 AGENTS 不属于独立代码库。
