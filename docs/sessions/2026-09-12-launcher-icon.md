# 桌面入口图标与状态定义

记录类型：本任务执行记录。状态：实现、验证、本地应用更新及源码推送完成。更新时间：2026-09-12 00:14（Asia/Singapore）。

## 目标与授权

用户要求改善桌面小图标，移除四周白边，使用新的图像生成做更精致的图标，并解释及明确右上角状态。范围为图标素材、入口窗口／样式、状态投影、验证和相关文档；实现阶段未提交／推送；随后用户明确授权交付，结果见下文。未真实采音。

## 接手基线

HEAD 38c70b1。此前已更新本地.app，且文档对齐和包检查记录尚未提交；本轮保留。用户截图显示蓝色波形图标、白色外沿和灰色圆点。源码确有白色button边框、状态点白边及不透明原生launcher背景。

## 实际变化与依据

使用内置image_gen生成无白描边的深蓝对话环形标识，状态标记由可信UI单独绘制。已接入生成PNG、透明窗口和统一状态投影，移除button／状态白描边及原生窗口阴影；点表示采集／服务健康，不以模型整理状态冒充采集状态。

## 验证与未验证

已查看用户图、生成图及实际44DIP入口；构建、40项单元／架构、9项Electron回归通过，合成五态逐一核对。原生截图角点alpha=0，中心为生成素材的接近不透明alpha。灰／连接／绿／暂停／错误及服务错误文字优先级已验证。绿色仅用合成快照／音源验证，不是用户真实录音。完整结果见[验证](../../tests/results/launcher-validation.md)。

## 未完项与下一步

本轮范围已完成：本地.app已重建，14个构建文件与包一致，实际新入口与原两条历史均已核对；工作页收起后保留桌面入口。Windows真机、复杂多显示器仍未测；本次未扩展Dock／系统托盘图标。

## 文件同步与交付

同步[素材及实际提示](../../src/ui/assets/README.md)、[五态规范](../design/launcher.md)、设计目录、前端／风格／验收／决策、源码导航、结果入口及状态／索引。图像由内置image_gen生成，素材复制进仓库。图标与此前文档同步随后一起交付，见下文；保留其他任务的在建内容。


## 后续授权：提交与推送

用户明确授权提交、推送图标与已完成的文档同步。交付前核对launcher-summary中的源码／依赖／素材SHA-256完全一致，沿用40项单元及9项桌面测试，文档与差异检查通过。飞书团队交接仍在另一任务编写，其在建session和共享索引行不纳入本次提交。已提交为`120f77c`并成功推送origin/main（远端由38c70b1前进至该提交）。交付状态说明随收尾文档提交同步。
