# 最新 PR 与会议 Event 评测体系

记录类型：本任务执行记录。
状态：本轮评测体系与可执行检查完成；真实模型／音频／Windows运行仍待证据。更新时间：2026-09-12 14:19 +08。

## 目标与授权

用户要求核对 GitHub 最新所有 PR，结合完整业务制作会议 Event 与 eval、指标和自动化检查；随后明确“两者都有，一起”。本轮新增合成会议语料、评测脚本、指标／需求映射与执行证据。允许隔离运行测试；不采集真实会议，不提交／推送／合并，不替换日常程序。

## 接手基线

已读项目入口、状态、session、产品定义、验收标准与协作运行时。GitHub PR #1/#2/#3 均已合并，2026-09-12 核对本地 HEAD 与远端 main 同为 `5878c204b0c3b0369a6236681e79306bb6919772`。本地有会中精修、试算、设计和文档未提交修改；保留。状态页及旧 PR 正文有历史滞后，不能当实时完成证据。

## 实际变化与依据

已建立24类Event、60条多轮语料（40中文／10英文／10混合）、30项指标和135项规范路由；复用现有真实领域／Electron测试，补充多轮语义数据及失败关闭的评分器。语义标注由本轮设计，未经用户人工审定，不能称为人工金标。

## 验证与未验证

分阶段复核：9项评分器、152领域、31完整Electron、构建与类型检查通过；360段全部保留／处理，66次替身调用、6600/3300 token，未知计量0；Windows/Mac app.asar及14个dist与同源独立构建逐字节一致。详见[验证](../../tests/results/event-evaluation-validation.md)及[机器摘要](../../tests/results/event-evaluation-summary.json)。

保留全部失败：隔离执行器漏HTML、依赖symlink构建路径、负载替身缺usage回调、新观测可选字段类型错误均已修正。最后完整运行151/152源于固定30ms流式测试等待；改为有界事件等待后全量152通过。唯一后续差异是该测试文件，产品与桌面代码指纹未变；不是一次完整全绿。真实模型未配置、调用0；语料不是独立人工金标。原始JSON导出包含协作数据，缺口是统一汇总及专项验证。

### Windows 与 macOS 同步状态（每轮必填）

| 平台 | 本轮影响与实现状态 | 本地包位置／架构／源码基线及一致性证据 | 实际验证与未验证 | 缺口与下一步 |
|---|---|---|---|---|
| Windows | 新增平台无关评测脚本与文档 | 不适用：不改变应用运行产物 | 同源Windows x64包14文件／app.asar匹配；本轮无Windows真机 | 保留真机与音源验证门槛 |
| macOS | 新增平台无关评测脚本与文档 | 不适用：不改变应用运行产物 | 9评分器、152领域、31Electron、构建及现有包严格匹配 | 真实模型／音频另验；日常包和进程不变 |

## 未完项与下一步

需要时在已配置模型环境运行`node scripts/evaluate.mjs --offline --model --release`，对60条多轮进行真实回放并独立审定标注／输出；真实音频、Windows原生及用户效果分层补证据，不冒充自动通过。完整双语、diff/tombstone、队列恢复、缺口补全和统一结束汇总等产品差距仍保留。本轮没有启动定时任务。

## 文件同步与交付

已更新评测规范、语料／指标／规范映射、脚本、README／fixtures／验收／结果入口、状态及session索引；全部仅本地。文档检查122份Markdown／33条session、格式与diff检查通过。未提交、推送、合并或替换日常包。新增测试／文档不改变两端运行产物，因此无本轮产品打包改动。
