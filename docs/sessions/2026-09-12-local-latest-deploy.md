# 本地最新版本部署

记录类型：本任务执行记录。
状态：本轮验证快照已部署并启动；当前工作树有后续并行改动，Windows真机待验。更新时间：2026-09-12T14:54:42+08:00。

## 目标与授权

用户要求部署本地最新版本并核对Git一致性；发现失败项后，用户明确选择“先修复本地失败项，再部署最新版本”。范围包括必要修复／回归、双端本地包和此Mac日常启动；未授权Git提交／推送、远程机器安装或真实录音。

## 接手基线

本地main／origin/main／实际GitHub main均为55267d6f903ca75a461e85ee3907c79479cf01aa，但存在会中精修、试算、Event评测及Demo实时可视化等未提交内容。提交相同不表示工作树相同。已读状态、规则、session流程和Demo／分支整合交接；保留已有成果。

## 实际变化与依据

建立独立源码快照构建、测试和打包。初轮发现重复schema挤占上下文至5115字节，长文本和协作草稿更新失败。修复前并行任务已写入schema引用复用、相应预算测试、局部保护和图形文字替代测试；逐项回读并纳入快照，不覆盖或归为本轮编写。随后并行写入流式DONE结束、图形反馈依内容变化而非对象引用触发及格式化，也已核对并定向回归。

本轮直接修正`tests/e2e/meeting-reminder.spec.ts`的HTTP转写替身配置：显式使用whisper-1，避免继承gpt-live-transcribe后错误连接WebSocket。日常模型／转写配置没有被本轮改变。

最终固定14:45的已验证快照。随后另一任务持续添加悬浮预览并重建共享release，导致日常启动期间应用路径短暂消失。保留这些新源码，未纳入本轮验收，也未宣称它们已部署。为防止继续覆盖运行文件，最终Mac安装到`~/Applications/Meeting Agent.app`；同源双端包保留到`release/deployed-20260912-1445/`，共享release根目录由并行任务继续维护。

## 验证与未验证

[完整证据](../../tests/results/local-deploy/README.md)：227项单元完整通过，之后相关9项单元通过；桌面初轮30/33、修复轮32/33，HTTP替身修正后受影响6项全部通过，失败原因与报告均保留。构建、双端打包、14个dist与三份app.asar一致性检查、Mac隔离启动通过。结果不覆盖之后的悬浮预览新实现。

日常应用已从独立安装路径启动，PID84954；通过macOS窗口列表确认主窗口和悬浮入口可见，保留应用数据目录；启动前0会议、无真实音频。原旧进程39735未响应正常退出和SIGTERM，在确认无活动会议后精确结束；共享包重建导致的临时进程84477也已结束。真实模型、音频质量及Windows真机未验。

### Windows 与 macOS 同步状态（每轮必填）

| 平台 | 本轮影响与实现状态 | 本地包位置／架构／源码基线及一致性证据 | 实际验证与未验证 | 缺口与下一步 |
|---|---|---|---|---|
| Windows | 本轮修复快照已构建，共享代码同源 | release/deployed-20260912-1445/win-unpacked，x64；55267d6＋14:45本地快照；packages.json | 构建、14文件及app.asar核对通过 | Windows真机运行待验；后续悬浮预览另同步 |
| macOS | 本轮修复快照已安装并启动 | ~/Applications/Meeting Agent.app，arm64；同源保留包在release/deployed-20260912-1445/mac-arm64；packages.json | 构建、隔离烟测、安装指纹与新进程通过 | 真实模型／音源未验；后续悬浮预览另同步 |

## 未完项与下一步

本轮部署快照已完成；当前工作树仍比该快照更新，后续悬浮预览及相关新变更需其任务完成验收和双端同步，见[source-drift.json](../../tests/results/local-deploy/source-drift.json)。不能将本轮完成写成当前移动工作树全部部署或双端真实验收。GitHub main相同但工作树有未提交内容；Git提交／推送须另有当前授权。

## 文件同步与交付

已更新本记录、状态／session索引和结果入口；用户Applications已实际安装，固定双端包已实际保存。没有提交／推送，没有修改其他任务完成状态。文档检查通过（134份MD、38条session），本轮差异格式检查通过；结果见验证目录doc-check.txt。
