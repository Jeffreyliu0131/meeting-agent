# 完整自然音频模拟验收

记录类型：本任务执行记录。
状态：部分完成，用户要求转为提交当前阶段。更新时间：2026-09-12（Asia/Shanghai）。

## 目标与授权

用户要求完整播放已有自然对话 TTS，观察真实采集、转写、组件提出、host 审核分发和本地参与者交互；后补充要求测试只采集本地播放音频、排除现场麦克风干扰。本轮没有授权提交、推送或替其他任务处理合并。

## 接手基线

已读状态、session 索引、组件缩略区记录、自然音频手工验收指南。已运行 Windows Electron 使用 `main-DuMq4MfD.js`；开始时工作树随后出现其他任务的 main 合并冲突，HEAD 从 ee5ccc3 变为 68e1c87。本轮不重建或改动运行源码，不把进行中的源码合并视为已经运行的版本。

## 实际变化与依据

- 创建忽略目录 `.cache/meeting-run.mjs`，通过现有 Electron CDP 读取快照、调用可信 IPC 和操作实际页面。
- 第一场播放 01、02：系统音频确实转写，但混入现场麦克风，随后发生 `TRANSCRIPTION_BACKLOG`，采集停止，第二段尾部不完整。保留为失败记录并结束采集。
- 根据用户纠正，新建仅系统音频测试场。现有产品没有仅系统音频设置，因此在本次采集页临时将 `getUserMedia` 替换为禁用轨道的静音流，不调用物理麦克风；`getDisplayMedia`、实际系统播放、STT 和 Agent 均保持原链路。此为临时测试隔离，不是产品已实现音源开关。结束后已恢复。
- 仅系统音频场没有麦克风转写。第01段和第03段实际出现 `TRANSCRIPTION_TIMEOUT`；重连后第03段的首段来源生成了一张默认投票，3项已填充，但第三项只有“先内部试用一周”，缺少“再邀请客户”。卡片进入ready并自动显示在固定缩略区；没有手填补正，也没有审核分发这个不完整版本。
- 第03段再次重播仍超时，没有新增最终转写；当前运行包约8秒提交一次，失败调用约30秒结束。这里只能证明已复现延迟和中断，尚未定位供应商与客户端各自原因，不能称已修复。
- 独立WebSocket诊断尝试在沙箱遇到 `EACCES`，没有产生服务端诊断证据。用户要求先提交后停止诊断。
- 两场测试已结束；临时静音替换已恢复，测试音频播放已停止。
- 原始快照仅在忽略目录，不把现场音频转写放入版本库。

## 验证与未验证

音源为 `exports/collaboration-tts-20260912` 的合成 TTS。实际用 Windows SoundPlayer 播放；实际模型为 gpt-live-transcribe 与 deepseek-flash。本轮只实际走到背景讨论及第一张自动投票；审核分发、参与者响应、后续分工／冲突／确认未完成。自然音频第04至10段未播放。旧文本／传输替身测试不替代本次真实音频验收。

### Windows 与 macOS 同步状态（每轮必填）

| 平台 | 本轮影响与实现状态 | 本地包位置／架构／源码基线及一致性证据 | 实际验证与未验证 | 缺口与下一步 |
|---|---|---|---|---|
| Windows | 运行验收和临时测试隔离；本音频阶段未改运行源码 | 当时已运行 dist 开发程序，main-DuMq4MfD.js | 真实系统音频与真实 API 部分完成；未通过完整闭环 | 已结束采集并恢复隔离；先排查转写，再从第01段重新完整测试 |
| macOS | 不适用：本轮未改变运行产物 | 未构建新包 | 未进行 Mac 实机音频验收 | Windows 观察不可当作 Mac 通过 |

## 未完项与下一步

排查实时转写积压和超时；提供正式的仅系统音频设置，再从第01段重新测试完整场，核验第三选项补全和后续审核分发／回应。不通过手填组件或注入台词冒充通过。用户已要求暂停本测试并提交当前阶段，后续执行需遵循新请求。

## 文件同步与交付

共享状态、session索引及结果索引已补入本记录链接。后续按用户请求提交当前阶段的整合、检查和PR见[提交交接](2026-09-12-component-checkpoint-pr.md)。不将本记录的旧运行包结果套用于后续构建版本。
