# 自动组件缩略区与悬停审核

记录类型：本任务执行记录。
状态：原分支缩略区实现与Windows重启完成；后续PR #4整合见[合并交接](2026-09-12-pr4-integration.md)，真实音频测试见[本轮实测](2026-09-12-full-audio-simulation.md)。日期：2026-09-12。

## 目标与授权

用户纠正展示方式：结合上下文与业务唤醒关键词，组件内容完整可用后自动出现于固定区域，以内容缩略卡呈现；悬停或点击查看完整卡片和审核页，host一键分发。按此明确要求实施，不再只提示数量。

## 接手基线

YH a91dcce及上一轮完整审核／自然音频等未提交改动全部保留。日常应用仍是此前启动的构建。本轮不提交、推送或自动采音；按用户要求排查当前后台后，确认无活动会议，再正常退出旧实例并重开修正程序。

## 实际变化与依据

现状只有launcher数量和悬停后的文字索引，必须手动寻找并打开组件。将新增屏幕右侧固定组件区，自动showInactive展示真实内容缩略图；同窗向左展开完整审核，悬停暂览、点击固定。复用既有可信host审核命令与四组件内容，未通过准备校验的草稿不冒出。

## 验证与未验证

先将四组件桌面验收改为自动缩略区路径，核对没有主动openComponent请求即可看到卡片；再验证悬停、点击、一键分发和未审批不公开。真实模型触发准确率及日常音频整段体验不由传输替身证明。

## 未完项与下一步

固定区、IPC权限、悬停／固定和逐段内容更新完成；构建与147单元通过，8相关桌面用例覆盖通过。实际DeepSeek两段生成同一投票rev1→rev2、两项→三项，最终ready且未公开，耗时10.5／13.765秒。修复JSON参数、低延迟调用与具体schema修复反馈；过程失败与证据范围见[结果](../../tests/results/component-dock-validation.md)。

后台旧实例可见转写内容、C的HTTP400、M超时；新程序PID23592已核对新脚本与component-dock窗口，12场历史保留、0活动会议。后续重测完整音频与转写超时；不能用本轮文本证据宣称实采完成。

## 文件同步与交付

更新现行前端／组件运行说明、状态与索引；保留此前音频和规划成果。
