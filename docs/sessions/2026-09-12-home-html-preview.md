# 初始首页高保真 HTML 预览

记录类型：本任务执行记录。
状态：第四版HTML完成，待用户视觉评审；前三版均未获认可。更新时间：2026-09-12（Asia/Singapore）。

## 目标与授权

用户明确要求先制作高保真前端HTML观看，认可后才做正式实现。仅制作独立首屏预览与必要交接；不改src、构建包、采集或模型，不提交／推送／发布。前轮只读分析推荐“开始会议＋成果预览”，本轮据此做可评审视觉稿；不要求先生成图片。

## 接手基线

HEAD 596c8b7。已读协作入口、状态、session索引、当前视觉与入口规范。会中工作页、个人试算及其他文档存在并行未提交改动，保留。独立目录docs/design/prototypes/home-v1与home-v2，有限编辑设计索引、状态和本session索引。

## 实际变化与依据

用户认为v3缩间距／灰色／圆角没有可感知提升，进一步选择“像飞书客户端：清爽、有层次，控件细节明显更讲究”。范围仍是基于当前首页的独立HTML，不做正式应用实现。

第四版保留顶栏、欢迎区、开始会议、收听范围、配置提示及最近会议的上下结构。明显改善点聚焦欢迎内容与操作区的表面分层、麦克风／电脑声音的独立控件、文字层次与完整状态，不引入侧栏工作台或大示例卡片。目录home-v4。

## 验证与未验证

工作树已合并5878c20；核对当前home-hero与start-row语义仍一致。延续v3的实际首页来源，修改仅在独立预览目录；当前产品并行整合工作保留。第四版已完成，11项交互检查与不同窗口截图核验通过，页面控制台无错误；见[第四版](../design/prototypes/home-v4/README.md)。v1–v3只是历史未认可方案，既有测试不覆盖本版。

### Windows 与 macOS 同步状态（每轮必填）

| 平台 | 本轮影响与实现状态 | 本地包位置／架构／源码基线及一致性证据 | 实际验证与未验证 | 缺口与下一步 |
|---|---|---|---|---|
| Windows | 独立跨平台HTML，正式产品未变 | 不适用：用户限定先预览，不改变运行产物 | Windows浏览器未实测 | 用户确认后才进入正式双端实现 |
| macOS | 独立HTML预览，已完成 | 不适用：未改变运行产物 | 第四版本地截图与11项通过 | 待用户视觉评审 |

## 未完项与下一步

本轮预览与验证完成。待用户确认视觉；确认前不做正式实现，后续接入需保留中英与Windows／macOS同步要求。

## 文件同步与交付

已更新设计索引／session索引／状态、预览说明与核验记录；正式源码、包和应用数据未改，未提交／推送／部署。文档检查通过（87个Markdown／23条session），git diff --check通过。


## 第二轮：飞书参考与会议首页重做

用户反馈：首版不够高级，缺设计感，偏AI卡片风；要求学习飞书，从第一性原理重做适合会议的实现。范围沿用独立HTML预览，未授权接入正式应用。首版保留供比较，不再作为待认可推荐方案。

已读取飞书官方云文档使用指南并检查其中的真实产品截图；作为视觉结构参考，不冒充当前登录态或所有最新版本。目标调整：稳定的轻侧栏、平面内容区、会议列表为主体、单一开始动作、声音范围邻近动作、没有首屏宣传大标题与样例卡片。首次空状态与有记录状态分别呈现，示例手动选择且有持续标记。

新文件范围：docs/design/prototypes/home-v2。已完成重构，18项交互检查通过，桌面／窄窗／200%截图完成。核验和来源见[第二版说明](../design/prototypes/home-v2/README.md)。Windows与macOS正式源码／包均不变，跨平台接入仍等待用户认可。


### 第二版交付结果

预览地址：http://127.0.0.1:4179/ 。默认6条明确标记的合成记录，底部可切首次空态、会中、设备异常；搜索和过滤、会议侧栏与原话、音频设置、模拟开始／暂停均可体验。正式src／包／会议数据未修改。

已实际查看飞书／Lark官方产品截图，并据此调整为会议工作台。修正元信息过淡、底部预留和弹窗内部点击误触关闭。18项检查通过，控制台无页面错误，界面照片已核验；用户尚未确认视觉。Windows／macOS本轮仍仅独立HTML，不改变正式运行产物，故两端重打包均不适用。新版引入的搜索内容和列表摘要属于待评审原型行为，正式接入时须以真实保存内容支持。

收尾更新本session、状态、索引和设计入口。旧版保留且已标注否定；未提交／推送／部署。


### 第三版交付

实际首页DOM/CSS与设置弹窗直接捕获后装入独立HTML，当前／精修切换只有样式变化。已通过结构／文案不变、短窗、设置模拟、原有列表与过滤、关闭恢复及200%等12项检查；截图已目视核对。依据来自飞书官方页面设计原则，采取4/8px节奏、统一圆角和字阶，不重构页面。

Windows与macOS正式代码和本地包均未改变；只读捕获与独立HTML不要求重打包。现有产品源码和其他任务工作保留。用户视觉认可尚待确认；未提交／推送／部署。

第三版收尾检查：本轮设计文档链接已接通，git diff --check通过。全库check-docs仍被并行出现的 `docs/sessions/2026-09-12-pr3-fix-merge 2.md` 未索引／不可达阻断；本轮未修改该其他任务副本，也不据此宣称全库文档检查通过。


第四版交付：127.0.0.1:4181，用户已在内置浏览器打开。按其选择实现浅色欢迎区／白色操作条、完整音源控件和统一最近会议区域；保留当前上下结构。用户询问电脑声音用途，已解释线上远端发言／电脑其他声音范围，并补充title说明。正式应用未改，视觉认可待后续反馈；未提交／推送／部署。
