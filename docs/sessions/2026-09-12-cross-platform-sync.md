# Mac 与 Windows 本地包同步

记录类型：本任务执行记录。
状态：本地包同步与GitHub交付完成；Windows真机待验。更新时间：2026-09-12 10:41（Asia/Singapore）。

## 目标与授权
用户要求检查清楚并做好两端同步。授权更新本地两端构建包、必要验证和文档；保留日常会议，不注入测试事件，不采真实音频或调用真实模型。同步完成后用户明确授权提交并推送本轮结果；应用包与日常数据库仍不入库。

## 接手基线
HEAD 6628213，产品代码9b6886d。已有飞书交接文件及状态／索引修改，完整保留。已读状态、相关前端／图标及工作流交接。旧release两端JS／CSS／worker不同，Windows缺新launcher素材；最新验收包曾一致不代表日常包已更新。

## 实际变化与依据
从当前源码统一构建，更新原位置Mac arm64和Windows x64目录包；离线使用Electron 44.3.0，旧包留临时回退副本。两端14个dist文件和整个app.asar一致，85项源文件指纹与独立验收一致。无产品代码修改。

## 验证与未验证
构建、双端打包、11项桌面回归、Mac包隔离及日常启动通过；已目视核对合成恢复与日常首页。结果见[本轮验证](../../tests/results/cross-platform-sync-validation.md)。当前日常会议前后均0条，未注入测试事件。跟随系统语言解析值由zh-CN变en，重建原hash确认其他保存偏好一致。Windows无可用真机，未做该端运行或音源验收；86项单元不重跑，仅核对前次验收源码指纹。

## 未完项与下一步
本地同步范围完成；Mac新包已打开。Windows需在实际设备使用整个win-unpacked目录并验收字体／DPI、窗口、权限和音源；当前没有连接可远程替换的Windows机器。代码／包同步不会同步各机器的本地会议历史。旧记录的两条历史不代表本机当前数据。

## 文件同步与交付
同步README当前包说明、状态、session索引及结果索引。最终`python3 scripts/check-docs.py`通过（63篇MD／16条session），`git diff --check`通过；已重读状态与索引，产品源码未变。同步结果已以`06f57d24bdad866cbd3787775e9a16c79d77655b`提交并推送`origin/main`，远端回读SHA一致。实际暂存树62篇MD／15条session检查通过，本轮12文件未含凭证模式、应用包或日常数据库；未修改飞书交接内容。本段交付状态随后补交，产品源码不变。
