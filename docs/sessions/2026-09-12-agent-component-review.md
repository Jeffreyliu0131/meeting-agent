# Agent生成组件与一键审核分发修正

记录类型：本任务执行记录。状态：本轮修正与音频交付完成，桌面测试进程退出问题待排查。更新时间：2026-09-12（Asia/Shanghai）。

## 目标与授权

用户明确纠正：预设组件由Agent直接生成，host只审核并一键分发，不能将填表、组装选项或配置规则作为正常路径；所有组件一致。本轮据此修正现有四类链路及测试口径，原TTS任务保留，在台词修正后继续。未授权Git交付。

## 接手基线

YH a91dcce及之前的规划、测试材料等未提交文档，保留。TTS探针被用户中断，输出目录尚无音频。

## 实际变化与依据

源码确认三处偏离：新会议不默认启用协作使M完全跳过组件意图；host首次打开直接进入编辑器并手选披露；C上下文主要取当前批次，前文方案容易缺失。另需让收集结束形成私有审核态、完整内容不再要求手工组装。

修正目标：正常音频会议默认有本地模拟身份；M/C使用会议上下文与默认交互规则准备完整组件；四类共用只读审核预览与默认受众／本次共享依据，host点击一次分发。编辑和手工建卡折叠为高级操作，未知必需事实用具体澄清处理。

用户进一步明确：产品是静默记录和分析，台词全程应是参会者之间自然讨论，不应有一句对Agent下命令。已将此作为触发规则与验收前提，指令式v1不再作为正常通过证据，替换为自然讨论v2。M根据现实协作需求推断suggested意图；无现实需求的闲聊、否定或历史引用不建卡。

## 验证与未验证

构建与145项单元通过。新桌面用例验证四类组件真实M/C/R路由和一键审核分发，模型传输为替身。实际DeepSeek使用合成自然摘录：闲聊无误触发、分工完整，投票与确认修复收集状态后均为ready且无公开轮。发现并修复C/R JSON-object接口缺少JSON字面导致400的问题。最终桌面回归及边界见[结果记录](../../tests/results/agent-component-review-validation.md)。

自然讨论v2已经替换正常测试材料；真实TTS生成14段单音色AI语音、5分17秒主线音频、分段播放器与ZIP（本地exports忽略目录）。验证WAV参数／时长／非静音；没有逐字回转写、真实麦克风或说话人识别验收。

## 未完项与下一步

本轮代码、台词与TTS修正已落地；最终26项桌面功能断言通过，但worker teardown超时、命令退出码1；保留此测试运行器未完项，不能标为整个回归命令通过。日常进程未重启，已有运行窗口仍是旧构建。下一步在新构建按分段音频做真实采音验收，重点观察跨批次准备、第三个方案、自然异议、只调整指定任务与确认状态；不通过额外Agent口令或手工补字段将失败记为通过。第二批组件开发仍未开始。

## 文件同步与交付

已同步产品定义／COL03、前端与首版运行规则、后续阶段约束、源码导航、测试材料及结果入口。结构检查通过；保留上轮已有未提交规划等文件，不提交、不推送、不替换日常应用。
