# Agent 工作流实施

记录类型：本任务执行记录。
状态：本轮W1–W6本地实现与程序验证完成；真实效果待验收。更新时间：2026-09-12 01:33（Asia/Singapore）。

## 目标与授权
完成W1–W6本地代码、UI、必要依赖、隔离合成测试与文档。未采真实音频、未调用付费模型、未操作日常会议数据库、未发送发布、未替换安装包、未Git提交推送，未启动其他任务或多Agent。

## 接手基线
HEAD a7d5771，可靠性代码8e0d2eb。接手时README、决策、计划、状态、索引及研究／业务／飞书交接已有未提交内容，均保留。读取入口、业务规范、W1–W6任务书、AC01–25、研究及相关源码后实施；原58／10记录仅作历史基线，并实际复跑58项单元通过。

## 实际变化与依据
- W1：可信ID映射、read set／CAS、持久任务／不可变提案／唯一成功批次、attempt／租约fencing、预算继承和恢复；失败输入隔离但不标成功。
- W2：独立轻量对象／产物目录，真实生产协议的主动证据分支，版本／原话／邻近／依赖／分页工具与coverage。
- W3：LangGraph实时／个人图，发送时冻结对象／产物基线、独立锁／取消／持久预算、会议调用保留槽与个人任务UI。
- W4：先查后问、持久澄清、个人回答／取消／自然来源消解／候选过期；closeout纳入任务及澄清状态。
- W5：范围与branch／purpose／有序对象身份，候选持久化、RenderReport、一次内容保持修复；单位校验、公式引文、可信计算绑定。个人产物依赖保留原对象版本。
- W6：生产HTTP＋Electron合成场景、3个恢复故障窗、30秒工具等待、最终全量检查、双端目录包、离线人工评分及源码hash。

稳定依赖为LangGraph1.4.14／core1.1.48；恢复使用SQLite显式状态，没有将MemorySaver称为持久化。具体接口、预算和有限支持范围见[运行时说明](../agent-workflow-runtime.md)与[ADR-005](../adr/005-bounded-agent-workflows.md)。研究报告保持研究当时结论，仅补后续入口。

自审实际修复：异步图节点使两个旧测试的同步启动假设失效；IPC白名单漏接新命令由真实Electron测出并修正；补齐所有新ID的服务分配、跨branch产物ID隔离、个人发送时基线及产物版本、CAS重规划预算继承、预览候选恢复与修复内容保护、个人取消不污染会议错误状态。

## 验证与未验证
最终81项单元、11项真实Electron合成检查通过；构建、格式和diff检查通过。macOS arm64／Windows x64目录包成功，各14个dist文件与包内hash一致；macOS最终临时包service ready。Windows仅构建，未实机运行。

详见[W1–W6→源码→AC证据表](../../tests/results/agent-workflow-validation.md)与[84文件源码指纹／机器结果](../../tests/results/agent-workflow-summary.json)。真实模型语义、真实音源、实际时延费用、Windows实机及完整辅助技术未验收；离线语义评分全部not_run。

## 未完项与下一步
本轮授权的本地实现与合成程序验证已完成。真实语义／音源与Windows实机需后续明确授权和环境；不自动调用模型或恢复录音。已实现检索为本会议词项／有界分页，非无限长会或外部RAG；修复保守限制布局／几何；公式引用匹配不证明数学关系正确；旧数值图有来源不等于可信计算。边界已写入运行时正文。

后续接手先核对Git与本次hash，再开展已授权工作。当前主程序安装包仍为此前版本，本轮包在临时目录；不要将源码交付误读为日常应用已升级。

## 文件同步与交付
已同步README、技术设计、契约、可靠性、表达、前端、入口、渲染运行环境、ADR、源码导航、任务书、测试结果、状态和索引；保留原有其他任务内容。文档检查见结果链接。未提交／未推送／未部署。
