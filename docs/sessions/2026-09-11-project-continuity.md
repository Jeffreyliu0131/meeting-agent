# 项目路由与跨 session 接续整理

记录类型：本任务执行记录。状态：本轮文档维护完成。
更新：2026-09-11 23:13（Asia/Singapore）。关联任务：审查 Agent.md 项目路由，ID `01a09100-375f-7422-acd5-1dfc5bc365f7`。

## 目标与授权

用户明确要求对齐整个项目文档，建立清晰 AGENTS 路由、现行进度与认知，并要求每轮迭代更新相关文档和具体 session 交接。范围为文档与必要的一致性工具，不含产品代码实施、采音、Git 提交／推送或发布。

## 接手基线

本地 HEAD `5fcd90f`，已有 Agent 迭代和会议入口规范任务同时运行。根入口仍写研究阶段，仓库入口混有已结束的只写文档约束；进度与验证散落，没有 session 索引。保留其他任务的代码和最新 UX 修订。

## 实际变化与依据

重写两层入口与根 README，建立状态页、任务路由、session 索引和模板，补录首版证据与两个并行任务的观察交接。用户最新入口认知从已写入专项规范及关联任务确认核对；不从旧产品版本推导新要求。

## 验证与未验证

实际执行于 2026-09-11，Python 3.14，本地 HEAD `5fcd90f` 加本轮文档／脚本及并行任务工作树；检查不把其他任务的产品改动计为已验证。

- `python3 scripts/check-docs.py`：通过。检查仓库 30 份 MD、4 份 session 记录；本地链接、独立仓库边界、入口路由、交接字段与认知文档可达性无错误。
- 父工作区 `python3 scripts/check_alignment.py`：通过。父目录 27 份 authored Markdown、12 份核心材料、10 个历史业务步骤、8 个历史报告章节；历史证据行和原图保持不变，同时调用仓库路由检查。
- `git diff --check`：通过，未发现差异空白错误。
- 按新 session 的入口实际读回两条路径：父工作区进入仓库，以及直接从仓库 AGENTS 进入状态／session／专项规范；均可找到产品目标、当前基线、进行中改造、最新 UX、未验收项和下一步。这是本任务的人工式复核，不冒充另一个 Agent 独立测试。

未运行产品、模型、音频或桌面验收；没有联网重验历史外部事实。检查脚本验证结构，不保证任意未来 Agent 必然遵守，也不能防止任务异常中断造成尚未写回的进度。

## 未完项与下一步

本轮路由、范围标注、阶段计划和文档检查已完成。下一步属于仍在运行的实现任务：核实 Agent 改造及已接收的会议入口规范接入结果，同步技术／契约／README 与新的测试记录；不由本任务代为宣布完成。会议入口规范任务已确认结束，但新流程产品验收未完成。

新 session 接手时先核对状态页、相关记录和 Git。当前状态是有限时点快照；不应凭本轮检查通过推断后来代码或文档仍保持一致。

## 文件同步与交付

已同步：两层 AGENTS、两层 README、状态页、session 索引／模板／4 份交接、实施计划、设计文件职责与旧 ADR 状态、源码目录说明、验证范围、父目录历史资料入口和阶段标记。保留并行任务的 UX01–UX07 与专项规范，记录 DOC01–DOC02 的维护要求。

必要工具：[check-docs.py](../../scripts/check-docs.py)；父目录的一致性脚本同步为历史报告检查并调用它。报告源码、数据和离线导出作为历史快照保留，未重生成；未改产品源码。

本地文档与维护脚本修改，未 Git 提交／推送／部署。状态、索引和本记录已收尾；并行任务的代码仍由其负责。
