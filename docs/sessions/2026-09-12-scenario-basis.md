# 已保存个人试算的依据变化提示

记录类型：本任务执行记录。
状态：代码／双端本地包已同步，macOS包内验证通过；Windows真机待验。更新时间：2026-09-12（Asia/Singapore）。

## 目标与授权
用户要求排除动态组件，从小到大先完成一项业务／体验改进。本轮修正已保存个人试算被无关新发言误标过期的问题；包含必要验证及双端本地包。不改Agent生成、动态组件、原话或保存数值；不采音、不调用真实模型、不提交／推送／部署。

## 接手基线
HEAD `596c8b7`。已读状态、索引、会中工作页与设置交接。保留已有正文／原话边注、Agent提示词、设计与规则改动；它们不归为本轮完成。共享main仅改保存试算提示，i18n补缺依据文案。所有既有产品工作树随同一dist进入本地包。

## 实际变化与依据
旧判断只比较`baseInputVersion < inputVersion`，任何新输入均触发。[独立判断](../../src/domain/scenario-basis.ts)现检查保存产物、公式、来源、对象／关系与传递条件，区分未变化、已变化、缺依据；保存值不变，无存储迁移。当前读集仍为保存产物粒度，尚无逐公式对象依赖。

## 验证与未验证
[完整结果](../../tests/results/scenario-basis-validation.md)：新增9项、全量114领域、源码Electron2项、实际Mac包2项和既有试算流程1项通过；build／格式／文档／差异通过。初次UI测试点击误开重命名、夹具类型及Windows默认架构已修正，失败记录保留。81个产品文件构建期间未变，双端14文件与app.asar一致。

### Windows 与 macOS 同步状态（每轮必填）

| 平台 | 本轮影响与实现状态 | 本地包位置／架构／源码基线及一致性证据 | 实际验证与未验证 | 缺口与下一步 |
|---|---|---|---|---|
| Windows | 共用判断与提示已实现 | release/win-unpacked，x64，596c8b7＋本轮及既有工作树；[指纹](../../tests/results/scenario-basis-packages.json) | 构建／PE架构／包内容通过 | Windows真机待验，未远程安装 |
| macOS | 共用判断与提示已实现 | release/mac-arm64/Meeting Agent.app，arm64，同一基线 | 领域／Electron／实际包检查通过 | 日常进程未重启；未采音 |

## 未完项与下一步
本轮修复及双端包完成，Windows真机待验。日常应用重启后加载新包。后续改进按用户选择，动态组件留在其他任务范围；真实模型的修正识别质量仍待独立验收。

## 文件同步与交付
已同步前端／契约／AC-17、源码／夹具导航、结果、索引和状态；仅本地代码及包，未Git提交／推送／部署。
